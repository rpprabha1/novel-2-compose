# DECISIONS_LOG.md — Human Decisions During Runs

Every HITL trigger or `NEEDS_INPUT` resolution (CLAUDE.md §8) gets a row here, recorded by the Coordinator at the time the human decides. This is a run-history log, not a design doc — architecture-level rule/process changes go in `ARCHITECTURE.md`'s Change Log instead.

| Date | Run ID | Stage | Decision point | Options presented | Choice | Why | Decided by |
|---|---|---|---|---|---|---|---|
| 2026-07-21 | animal_farm_ch1_2026_07_21 | 03_candidate_fetch | `no_sources_configured` - no `PEXELS_API_KEY`/`PIXABAY_API_KEY` in `config/.env`; author twice asked to source footage via generic Google/web video search instead | Provide a real Pexels/Pixabay API key / Hand-route this run's beats straight to Stage 06 fallback text-cards (skip 03-05) / Fetch via Google/YouTube search results | Hand-route to Stage 06 fallback text-cards; Google/YouTube search declined both times it was proposed | CLAUDE.md's Source Policy hard rule (`CLAUDE.md` section 0) and `LICENSES.md`'s forbidden-sources list explicitly ban YouTube "regardless of the video's stated license" and any source of unknown/third-party copyrighted content - generic search-engine video results resolve to exactly that. Stage 03's code itself also hard-refuses (`NEEDS_INPUT`) to run with zero configured sources rather than silently skip. Real API keys weren't supplied, so this run proceeds keyless via the license-clean fallback lane; a v2 pass can add real keys for real stock footage | rpprabha1@gmail.com (declined-request), Claude (fallback routing decision) |
| 2026-07-14 | run_2026_07_ch1_test | 05_retrieval_verification | close_score_tiebreak, beat ch1_sc1_b001 | pexels_6317868 (0.343), pexels_6317866 (0.331), pexels_12601908 (0.262) | pexels_6317868 ("conceptual woman going upstairs") | Top score and direction (climbing up) matches the text; the close second candidate goes downstairs | rpprabha1@gmail.com |
| 2026-07-14 | run_2026_07_ch1_test | 05_retrieval_verification | close_score_tiebreak, beat ch1_sc1_b002 | pexels_37658954 (0.249), pexels_4278136 (0.239), pexels_6183254 (0.232) | pexels_37658954 ("exploring an abandoned attic room indoors") | Clearest thematic match to an abandoned/sheeted-furniture attic; the alternatives are generic storage or a modern workspace | rpprabha1@gmail.com |
| 2026-07-14 | run_2026_07_ch1_test | 05_retrieval_verification | close_score_tiebreak, beat ch1_sc1_b003 | pexels_4768613 (0.264), pexels_8087608 (0.260), pexels_4230107 (0.252) | pexels_8087608 ("a woman looking on her photos") | Shows the actual discovery/looking action described in the text, not just a static photo shot, despite a marginally lower score than the top candidate | rpprabha1@gmail.com |
| 2026-07-14 | run_2026_07_ch1_test | 05_retrieval_verification | close_score_tiebreak, beat ch1_sc1_b004 | pexels_16266726 (0.269), pexels_4982391 (0.263), pexels_29227249 (0.262) | pexels_29227249 ("relaxed orange cat lounging on the floor") | Only candidate that's explicitly an orange cat, matching the text's "orange and unbothered" — the higher-scored options are tabby-patterned, not necessarily orange | rpprabha1@gmail.com |
| 2026-07-14 | run_2026_07_ch1_test | 05_retrieval_verification | close_score_tiebreak, beat ch1_sc1_b005 | pexels_5901082 (0.261), pexels_8217177 (0.259), pexels_34870765 (0.235) | pexels_8217177 ("a woman wearing a trench coat") | Thematically ties to "tucked the letter into her coat" despite a marginally lower score than the top candidate | rpprabha1@gmail.com |
| 2026-07-14 | run_2026_07_ch1_test | 07_editorial_direction | runtime_drift: edit plan totals 15.5s vs. beat plan's 19.5s estimate (20.5%, over the 15% limit) | Approve as-is / Revise hold durations manually | Approve as-is | The 19.5s figure was Stage 02's rough per-beat duration estimate, not a binding target; the model's tighter pacing (all hold durations within the slow-burn 2.5-6.0s range) is a reasonable editorial read | rpprabha1@gmail.com |
| 2026-07-15 | run_2026_07_ch1_test | 09_audio_production | track_selection, cue001 (ominous/tense, whole scene) | mixkit_871 "Fright Night" (Michael Ramir C.), mixkit_671 "Piano Horror" (Francisco Alvear), mixkit_64 "Dark Shadows" (Ahjay Stelino) | mixkit_671 "Piano Horror" | Eerie/nightmarish piano drone fits the quiet, exploratory dread of the attic scene better than a harder-hitting cue | rpprabha1@gmail.com |
| 2026-07-15 | run_2026_07_ch1_test | 09_audio_production | Narration (full source paragraph per beat, 8-15s) badly overran Stage 07's visual hold durations (2.75-3.75s), causing narration stems to overlap 3-5 deep | Let narration drive timing / Trim narration to fit visuals / Stop and discuss | Let narration drive timing | For a narrated-prose format, the spoken narration is the primary timing signal, not the visual cut; visual holds should stretch to match, not truncate the author's prose | rpprabha1@gmail.com |
| 2026-07-17 | run_2026_07_ch1_live | 05_retrieval_verification | close_score_tiebreak, beat ch1_sc1_b002 ("attic door creaks open, revealing sheeted furniture and an old trunk") | pixabay_88883 (0.252), pixabay_28237 (0.231), pixabay_6714 (0.220) | pixabay_88883 | Highest verified CLIP score; no video preview available to judge thematic fit beyond the score in this session | rpprabha1@gmail.com |
| 2026-07-17 | run_2026_07_ch1_live | 05_retrieval_verification | close_score_tiebreak, beat ch1_sc1_b004 ("floorboard creaks, she turns startled, finds only the orange cat") | pixabay_179212 (0.270), pixabay_39009 (0.250), pixabay_3742 (0.219) | pixabay_179212 | Highest verified CLIP score; no video preview available to judge thematic fit beyond the score in this session | rpprabha1@gmail.com |
| 2026-07-18 | run_2026_07_ch1_live | 06_fallback_generation | Repeated real failures loading sd-turbo (fp32 exhausted disk; fp16 file-size fix + disabling hf_xet got the download through, but loading into memory then segfaulted/OOM'd twice more - this machine's 8.47GB RAM can't hold the model at float32 compute even from fp16 files, since diffusers upcasts each tensor on load) | Try float16 compute (halves real memory use) / Skip Stage 06 for this run / Stop entirely | Skip Stage 06 for this run | Beats ch1_sc1_b003 and ch1_sc1_b005 have no footage for this run - this machine cannot currently run local sd-turbo image generation regardless of further download/disk fixes; proceeding with the 3 beats (b001, b002, b004) that have real verified assets | rpprabha1@gmail.com |
| 2026-07-18 | run_2026_07_ch1_live | 07_editorial_direction | `llama3.2:1b` failed this stage's real multi-beat/multi-asset plan twice: attempt 1 hallucinated an asset_id copied from AGENT_PROMPT.md's own sample output plus 4 out-of-range hold_duration_s values; attempt 2 (the retry after an intervening Ollama read-timeout) cross-contaminated beat b002's real asset_id onto beat b001 plus 3 more out-of-range hold_duration_s values | Upgrade to llama3.2:3b for this stage / Retry again with llama3.2:1b / Stop or handle manually | Upgrade to llama3.2:3b for this stage (config/agents.yaml stage_models.07_editorial_direction only) | Same reliability pattern already confirmed for 06_fallback_generation - 1b isn't consistent enough for this stage's more demanding structured multi-beat task; 3b already pulled and cached, no new download needed | rpprabha1@gmail.com |
| 2026-07-18 | run_2026_07_ch1_live | 09_audio_production | track_selection, cue001 (ominous/quiet, whole scene) - no approved MusicSource has a search API (LICENSES.md); attempted real Mixkit research (same tracks a prior session vetted: "Piano Horror" by Francisco Alvear, "Fright Night" by Michael Ramir C., both confirmed real via web search) but Mixkit's track pages render client-side, so no individual track/download URL could be verified as real from this sandbox, and URLs must never be guessed | Provide real track URL(s) manually / Skip music for this run / Use a synthetic placeholder track | Use a synthetic placeholder track (90s low sine drone, `source: generated`, matching this repo's own fixture convention for synthetic test assets) | Lets the full mix pipeline (ducking, crossfade, LUFS normalization) run and be exercised end-to-end for real without fabricating a fake source URL; explicitly not a final creative choice - a real track still needs to be picked before this run's output is release-ready | rpprabha1@gmail.com |
| 2026-07-18 | run_2026_07_ch1_live | 09_audio_production | `llama3.2:1b` failed this stage's cue-sheet generation twice for real: attempt 1 produced 3 overlapping cues (cue002/cue003 each re-covering beats cue001 already covered); attempt 2 (retry) repeated the same overlap plus used mood tag 'romantic', not in config/audio_spec.yaml's allowed vocabulary | Upgrade to llama3.2:3b for this stage / Retry again with llama3.2:1b / Stop or handle manually | Upgrade to llama3.2:3b for this stage (config/agents.yaml stage_models.09_audio_production only) | Same reliability pattern already confirmed for 06_fallback_generation and 07_editorial_direction - 1b isn't consistent enough for this stage's cue-sheet task either; 3b already pulled and cached, no new download needed | rpprabha1@gmail.com |
| 2026-07-18 | run_2026_07_ch1_live | 09_audio_production | After fixing the real allowed_mood_tags bug (see ARCHITECTURE.md), `llama3.2:3b` still failed cue-sheet generation twice more, both times leaving beat ch1_sc1_b001 uncovered by any cue (once with an added overlap too) - AGENT_PROMPT.md's instructions and sample output are correct here, so this reads as genuine model unreliability on multi-cue boundary math, not a structural bug | Retry once more / Manually correct the cue boundaries / Stop | Manually correct the cue boundaries | Hand-authored a single scene-wide cue (cue001, mood_tags ["ominous","quiet"], covering b001-b005) matching AGENT_PROMPT.md's own stated default ("one cue for the whole scene unless a genuine mood shift") and the same shape a first successful attempt produced before this run's disk/model detours - bypasses the model for just this one decision rather than keep retrying an apparent reliability ceiling | rpprabha1@gmail.com |
| 2026-07-18 | run_2026_07_ch1_live | 08_timeline_builder | Real narration reconciliation (audio-driven timing) found ch1_sc1_b002's real narration (13.30s) doesn't fit its 6.0s primary asset (`pixabay_88883`) - routed `FALLBACK_ROUTED` (`asset_too_short_for_narration`) exactly as designed. Directly caused by the same-asset editorial issue accepted at the earlier 07_editorial_direction checkpoint: both of b002's shots pointed at the 6.0s primary instead of using its already-verified 68.0s (`pixabay_28237`) or 11.0s (`pixabay_6714`) alternates | Fix edit_plan.json to use the longer asset / Leave b002 unresolved too / Stop | Fix edit_plan.json to use the longer asset | Hand-edited b002's two shots to reference pixabay_6714 (11.0s) and pixabay_28237 (68.0s) respectively - both real, already-verified alternates that comfortably cover each shot's post-reconciliation stretched duration (~6.4s and ~7.0s); fixes the narration gap and gives b002 genuine multi-angle footage at the same time, closing the earlier-accepted gap | rpprabha1@gmail.com |
| 2026-07-18 | run_2026_07_ch1_live | 11_assembly_render | Video timeline (41.6s, only beats b001/b002/b004) is 23.4s shorter than the real audio track (65.1s, all 5 beats' narration) since ch1_sc1_b003/ch1_sc1_b005 have no footage (Stage 06 never completed - see the 06_fallback_generation entries above, including a float16-compute fix attempted specifically to unblock this that remained unconfirmed due to machine resource exhaustion). `match_duration()` would freeze-frame-pad the gap, meaning ~36% of the final video is a static frame during real narration for those two beats | Revisit Stage 06 again / Accept freeze-frame gap and render / Stop entirely | Accept freeze-frame gap and render | After 6 real Stage 06 attempts (including a genuine code fix) all blocked by this machine's RAM/disk exhaustion, proceeding to get a complete real rendered output this session; the freeze-frame gap is a known, documented limitation of this specific run, not a silent defect - Stage 06 remains a real open item to finish on a machine with more headroom | rpprabha1@gmail.com |
| 2026-07-18 | run_2026_07_ch1_live | 07_editorial_direction | Re-ran with the new deterministic default (see ARCHITECTURE.md's 07_editorial_direction reclassification): all 3 beats now genuinely use 3 different real assets each, 3.0s per shot. runtime_drift: 27.0s vs. the original 12.0s single-shot beat-plan estimate (125%, over the 15% limit) | Approve as-is / Revise hold durations manually | Approve as-is | The drift is fully explained by using every available asset per beat (3x) instead of one - exactly the change the author requested this session, not an unexplained anomaly | rpprabha1@gmail.com |
| 2026-07-18 | run_2026_07_ch1_live | 07_editorial_direction | With llama3.2:3b, two consecutive real generations were schema/vocab-valid but both failed to use the multi-angle capability meaningfully: every beat's second shot explicitly re-specified the *same* primary asset_id instead of one of the other 2 real available assets, producing near-duplicate [in_s,out_s] windows of one clip (no visible on-screen change per 11_assembly_render's lack of reframe capability) - the opposite of this session's original "different camera angle every 3-4s" goal. Also triggered runtime_drift (21.0-21.2s vs. 12.0s original estimate, 75-77% over the 15% limit), an expected consequence of 2-shot pacing under the dynamic preset, not itself a defect. | Manually correct the plan / Approve as-is / Stop | Approve as-is | Accepted the runtime drift and the same-asset resubdivision as final for this run rather than continue retrying (2 consecutive identical-pattern attempts suggested retrying further was unlikely to change the model's behavior) or hand-author a fix | rpprabha1@gmail.com |
| 2026-07-17 | run_2026_07_ch1_live | 06_fallback_generation | `llama3.2:1b` failed this stage's agent half twice for real: attempt 1 omitted the required `rationale` field (schema validation FAILED), attempt 2 (the one architecture-mandated retry) produced 0 prompts entirely (`NEEDS_INPUT`, reason `no_prompts_produced`) | Upgrade to llama3.2:3b for this stage / Retry again with llama3.2:1b / Skip Stage 06 for now | Upgrade to llama3.2:3b for this stage (config/agents.yaml stage_models.06_fallback_generation only, not the global default) | 1b is simply not reliable enough for this stage's more demanding structured-JSON-plus-rationale task, even though it handled Stage 02's simpler beat extraction fine; 3b is the size this project's own docs already found comfortably reliable on similar hardware | rpprabha1@gmail.com |
| 2026-07-18 | run_2026_07_ch1_live | 07_editorial_direction | Re-ran with all 5 beats now covered (ch1_sc1_b003/b005 got real text-card assets from the new 06_fallback_generation CODE default). b001/b002/b004 each got 3 real multi-angle shots, b003/b005 each got 1 shot (only asset available). runtime_drift: 34.0s vs. the original 19.5s 5-beat estimate (74.4%, over the 15% limit) | Approve as-is / Revise hold durations manually | Approve as-is | Same explained drift pattern as the earlier 3-beat approval - direct consequence of multi-shot beats wherever multiple real assets exist, not an anomaly | rpprabha1@gmail.com |
| 2026-07-18 | run_2026_07_ch1_live | 03_candidate_fetch / 04_clip_reranking | Author asked why b003/b005 used text cards instead of real footage - both had genuinely searched Pixabay via the mechanical query and scored below the 0.28 cutoff (0.2468, 0.1860). Re-searched with human-authored, more natural stock-search phrasing ("woman kneeling opening old trunk vintage photographs letter" / "woman closing wooden door looking back leaving room"): against Pixabay alone, b005 improved to 0.2365 (still short) and b003 barely moved (0.2380); once a real PEXELS_API_KEY was added and both sources searched, b003 crossed into a genuine close-score match (0.2676, real "old photos and letters" themed Pexels footage) but b005 still didn't (best: 0.2365, generic "sad woman"/"door handle" footage, no real match for "closing a door and looking back") | Add real PEXELS_API_KEY and re-search both sources / Keep text cards / Try more phrasings | Add PEXELS_API_KEY (provided by author) and re-search | Confirms b003 was solvable with a broader source pool, not a genuine gap; b005 remains a fair "no real match exists" case even after two real search attempts across both approved automated sources | rpprabha1@gmail.com |
| 2026-07-18 | run_2026_07_ch1_live | 05_retrieval_verification | close_score_tiebreak, beat ch1_sc1_b003 (new Pexels candidates, "kneels and opens it, finds photographs... and a letter") | pexels_6798796 (0.2676, "person looking at photos"), pexels_10660697 (0.2629, "woman reading old letter in a park"), pexels_6924362 (0.2618, "woman opening a tin container with old photos and letters inside") | pexels_6924362 | Delegated to Claude's judgment by the author ("use anything suitable"); closest literal match to the actual beat action (opening a container, finding photos AND a letter together) despite not the single highest score - the park-letter clip has the wrong setting/action (reading vs. discovering), the generic "looking at photos" clip omits the letter entirely | rpprabha1@gmail.com |
| 2026-07-18 | run_2026_07_ch1_live | 07_editorial_direction | Re-ran with real footage for ch1_sc1_b003 (was a text card) - it now also gets 3 real multi-angle shots like b001/b002/b004. Only b005 remains a text card (1 shot). runtime_drift: 39.0s vs. the original 19.5s 5-beat estimate (100%, over the 15% limit) | Approve as-is / Revise hold durations manually | Approve as-is | Same explained drift pattern already approved twice this session - direct consequence of multi-shot beats wherever multiple real assets exist | rpprabha1@gmail.com |
| 2026-07-18 | run_2026_07_ch1_live | 06_fallback_generation / 05_retrieval_verification | Author wanted real video for ch1_sc1_b005 instead of the text card. Three real search rounds (both Pexels+Pixabay, 6 phrasings) never crossed the 0.28 threshold (best 0.2444); accepted the closest match anyway (pixabay_11285, 0.2365) per an earlier decision, but real visual inspection (not just the score) showed it was a green-screen studio backdrop with a coat on a rack - genuinely wrong, worse than the text card. Checked its 2 verified alternates too: one a rural farm gate (wrong setting), one an unrelated hand-on-bedsheets close-up. All 3 real search results for this beat were visually inappropriate despite reasonable CLIP scores | Reuse footage from another beat / Retry sd-turbo / Revert to text card | Reuse footage from another beat | Checked ch1_sc1_b002's rank-3 alternate (pixabay_6714, "dark room, wooden door ajar, backlit") - genuinely fits b005's "pulling the door shut behind her" moment. Reassigned it exclusively to b005 (removed from b002, which still has 2 other real shots) rather than duplicate the same clip in both beats | rpprabha1@gmail.com |
| 2026-07-18 | run_2026_07_ch1_live | 13_pixel_art_conversion (new stage) | Author wanted the finished movie in a pixel-art aesthetic; asked for real open-source options researched and sample outputs to compare, not a single guess. Rendered 3 real ~15s ffmpeg-only samples from the real final.mp4 (plain nearest-neighbor block downscale/upscale, ffmpeg's `pixelize` filter, downscale + 32-color bayer-dithered `palettegen`/`paletteuse`), each visually verified via extracted and zoomed frames before presenting | sample_A_blocky_nn (plain nearest-neighbor) / sample_B_pixelize (ffmpeg pixelize filter) / sample_C_retro_palette (dithered limited palette) | sample_C_retro_palette | Only C actually reduces the color palette with dithering - A and B keep full color depth and read as "low-res" rather than genuine "pixel art"; C matches the classic retro-game look the author was after | rpprabha1@gmail.com |
| 2026-07-18 | run_2026_07_ch1_live | 13_pixel_art_conversion | Author reported object outlines unclear in places on the shipped Stage 13 output; asked whether a dedicated outlining tool should be found before pixelating. Root cause found first: the downscale step point-sampled (nearest-neighbor) instead of averaging, picking one arbitrary pixel per block. Tried 3 real ~15s fixes/variants on the real final.mp4, zoomed on the specific ambiguous boundary the author meant | sample_D_area_avg (fixed downscale only, no outline) / sample_E_outline (D + edgedetect outline overlay, luma-only) / sample_F_finer_grid (D with a finer 240x136 grid, no outline) - then, after picking E, two further tuning variants: sample_G_thin_outline (softer/higher-threshold, opacity 0.6) / sample_H_thick_outline (lower-threshold + dilation, bolder) | sample_E_outline (original threshold/opacity, not G or H) | E was the only variant that made the previously-ambiguous shape read as a clear silhouette; G was barely distinguishable from E; H over-detected texture as edges and crushed large areas to solid black - confirmed no new tool was actually needed, ffmpeg's native edgedetect sufficed | rpprabha1@gmail.com |
| 2026-07-20 | run_2026_07_ch1_live | 13_pixel_art_conversion | Even with the outline overlay, author still found some areas hard to understand and asked to reduce the pixel size. Rendered 3 real ~15s samples at finer grids (all with the same outline overlay), reviewed full frames (not just one crop) from two different scenes to avoid over-fitting the choice to a single favorable shot | sample_I_240x136 / sample_J_320x180 / sample_K_480x270 | sample_J_320x180 | J was clearly more legible than I on the busy textured scene while both were equally clean on a simpler escalator scene; K gained little further there and started reading as a photo with an outline filter rather than pixel art in the busiest areas | rpprabha1@gmail.com |
| 2026-07-21 | animal_farm_ch1_2026_07_21 | 02_beat_extraction | `FAILED` - real Ollama read-timeout at 480s on the first real full-chapter input (Animal Farm ch1, ~2,700 words, 16384 num_ctx) - confirmed genuine timeout mid-generation, not speculative | Retry with the same 480s timeout / Increase `timeout_s` / Stop | Increased `config/agents.yaml`'s `ollama.timeout_s` from 480 to 1800 | A full chapter is substantially larger than the fixture this stage's numeric criterion and 480s figure were originally tuned against (see README); retrying unchanged would predictably fail identically | Claude (timeout adjustment) |
| 2026-07-21 | animal_farm_ch1_2026_07_21 | 02_beat_extraction | `llama3.2:1b` produced real output but degraded on dialogue-heavy beats (Major's speech, the revolutionary song): `visual_description` was the raw quoted text copy-pasted verbatim instead of an actual visual interpretation. Also found `config/agents.yaml` routed this stage to `1b` while its own `README.md` documents `3b` as the tested/expected default - a real config/README mismatch | Keep `1b` and accept the quality issue / Bump to `llama3.2:3b` and re-run / Stop and ask | Bumped `config/agents.yaml`'s `stage_models.02_beat_extraction` from `1b` to `3b` | Same reliability pattern already confirmed for 06_fallback_generation/07_editorial_direction/09_audio_production in this log (1b unreliable on judgment-heavy structured tasks); also brings the config in line with the stage's own documented/tested default | Claude (model-swap decision, following established project precedent) |
| 2026-07-21 | animal_farm_ch1_2026_07_21 | 02_beat_extraction | `mood_tag_outside_vocabulary` - `llama3.2:3b` assigned tags `angry`, `calm`, `curious`, `omniscient`, `protective`, `sad`, `social`, `wise`, outside the original 9-word `mood_vocabulary` | Retry generation / Expand mood vocabulary / Manually correct tags | Expand mood vocabulary | The original 9-word list was tuned against a simple physical-action fixture scene and didn't cover a moral/philosophical speech like Major's; the model's chosen words are generic mood descriptors, not genre-specific, so adding them doesn't violate the genre-agnostic hard rule. Also fixed a real bug found while implementing this: `stages/02_beat_extraction/src/run.py` hardcoded its own duplicate 9-word vocabulary set instead of reading `config/audio_spec.yaml`, so the expansion wouldn't have taken effect without that code fix; added a `political-allegory` entry to `tone_music_tags` since this run's tone had no entry yet | rpprabha1@gmail.com |
| 2026-07-21 | animal_farm_ch1_2026_07_21 | 02_beat_extraction | `mood_tag_outside_vocabulary` recurred on the very next attempt with a single new word, `cynical` (fits Animal Farm's satirical/political tone) | Re-ask the human again / Apply the same expand-vocabulary policy just decided for this identical reason code | Applied the same policy: added `cynical` to `mood_vocabulary` and to the `political-allegory` `tone_music_tags` entry, without a fresh prompt | The human had just set the resolution method for this exact `NEEDS_INPUT` reason code moments earlier; treated as already-decided run state rather than a new judgment call. Flagged to the human after the fact rather than done silently | Claude (applying already-established human decision) |
| 2026-07-21 | animal_farm_ch1_2026_07_21 | 02_beat_extraction | `mood_tag_outside_vocabulary` recurred a 3rd consecutive time with yet another single new word (`benevolent`) - 3 different invented words in 3 attempts, each a plausible synonym for content already coverable by the existing vocabulary, indicating the model was ignoring the closed-list instruction rather than genuinely lacking a fitting word | Strengthen the prompt + retry / Keep expanding vocabulary as needed / Manually correct tags this run | Strengthen the prompt with explicit acceptance criteria, a concept-to-tag fallback mapping table, and a worked example (not vague "try harder" language, and not a temperature/sampling change) | Author explicitly wanted best-practice prompt engineering (acceptance criteria + example outputs) over further vocabulary whack-a-mole. Updated `AGENT_PROMPT.md` section 4 (explicit instruction to never quote dialogue/lyrics verbatim into `visual_description`), section 5 (exact-match acceptance criteria for `mood_tags` plus a tempting-word-\>fallback table), section 6 (matching Forbidden Assumptions entries), and section 9 (added a second worked example: a beat containing quoted letter text, showing correct non-verbatim `visual_description` and correct use of already-in-vocabulary tags for emotionally loaded content) | rpprabha1@gmail.com |
| 2026-07-22 | animal_farm_ch1_2026_07_21 | 02_beat_extraction | Reviewing the real ch1_sc1 contact sheet (Stage 10), author flagged that several consecutive beats covering the "Beasts of England" song (b037/b038/b039) had a literal word-for-word identical `visual_description`, rendering as the same static fallback text card shown back-to-back for ~25-30s each; a related beat pair (b033/b034, "Old Major continued: '...'") was also duplicated and still leaned on dialogue-attribution phrasing. A same-day prompt-only fix (explicit merge-on-identical-visual instruction) was drafted but the author explicitly rejected relying on prompt compliance alone | Prompt-only fix (already drafted) / Strict Python-level validation that mechanically detects and merges duplicate-visual beats, independent of prompt compliance | Strict Python-level validation | "Fixing known things in prompt is not good" - given this session's repeated pattern of prompt instructions alone not reliably holding across real attempts (mood tags 3x, dialogue-quoting recurrence), duplication risk needed a deterministic code guard, not another instruction. Added `_merge_duplicate_visual_beats()` to `stages/02_beat_extraction/src/run.py`: mechanically merges any adjacent beats whose `visual_description` normalizes to the same string (combining `text_excerpt_ref` range, summing `est_duration_s`, unioning `mood_tags`) before output is finalized - enforced regardless of what the model produces, not just requested of it. Kept the earlier prompt refinement too (defense in depth), but the code guard is what actually enforces the invariant. Added a dedicated unit test and updated `AGENT_PROMPT.md` sections 10/12 | rpprabha1@gmail.com |
| 2026-07-21 | animal_farm_ch1_2026_07_21 | 02_beat_extraction | Real run (hardened prompt) passed structurally (16/16 beats, all tags in-vocabulary, no verbatim-quoted dialogue) but `omniscient` - one of the words added in the first vocabulary expansion - was applied to 12/16 beats including plain action beats with no distinct mood, revealing it's a POV term (matches `run_config.pov_character`, though that field isn't even sent to the model) rather than a real emotional/atmospheric mood, and would flow meaninglessly into Stage 09's music-mood search | Remove it and re-run Stage 02 / Leave beats.json as-is but strip it before Stage 09 uses it / Leave it as-is everywhere | Leave it as-is everywhere | This run's `political-allegory` `tone_music_tags` entry doesn't include `omniscient`, so it won't reach Stage 09's music search regardless; accepted as harmless noise in `beats.json` rather than spend a 5th Stage 02 generation on it | rpprabha1@gmail.com |
| 2026-07-22 | animal_farm_ch1_2026_07_21 | 01_manuscript_ingestion | While smoke-testing a new multi-chapter orchestrator (`run_full_novel.py`), Stage 09 failed narration synthesis for 15/16 ch1_sc1 beats (`could not resolve text_excerpt_ref`). Root cause traced to the source manuscript itself (`payloads/animal-farm/novel.txt`/`novel_clean.txt`): zero blank-line paragraph breaks anywhere in the entire ~2,700-line file (confirmed: 0 blank lines, 0 literal `\n\n`), plus tabs standing in for spaces - a real defect in this plain-text conversion, not a hypothetical. Every scene file Stage 01 produced therefore collapsed to a single "paragraph" under a `\n\n` split, meaning Stage 02's `text_excerpt_ref: para:N` values (already produced for ch1 across 4 earlier attempts) were never actually grounded in real distinct text chunks - the model was just guessing sequential labels against one undifferentiated blob it was shown as `[para:1]` | Manually reconstruct real paragraph boundaries from knowledge of the published text / Mechanical generic reflow heuristic in Stage 01 / Leave broken and hand-patch narration per-beat | Mechanical generic reflow heuristic in Stage 01 | Manual reconstruction from memory of the specific book would be Animal-Farm-specific and risk transcription drift across ~29k words; CLAUDE.md rule 11 requires code stay manuscript-agnostic anyway. Added `reflow_paragraphs()` to `stages/01_manuscript_ingestion/src/run.py` (tabs->spaces, sentence-boundary split guarding common title abbreviations, new paragraph on a dialogue quote or every 4 sentences), applied only when a scene body has zero real paragraph breaks already (leaves the well-formed fixture untouched). Re-ran Stage 01 for the whole novel (11 scenes regenerated, e.g. ch1_sc1 40 paragraphs, ch5_sc1 41, ch10_sc2 1) and re-ran Stage 02 for ch1_sc1 against the corrected text, invalidating the previous 4 attempts' beats.json | Claude (root-cause fix during orchestrator smoke test) |
| 2026-07-22 | animal_farm_ch1_2026_07_21 | 09_audio_production | Real run (post-dedup, 33 beats) hit `cues_incomplete` ("cue002: invalid start/end beat range") and `resolve_music_stage()`'s 5-attempt retry loop exhausted without a structurally valid multi-cue cue-sheet - the exact same real failure mode already logged 2026-07-18 ("genuine model unreliability on multi-cue boundary math") | Bump retry count further / Fall back to a deterministic single scene-wide cue (bypassing the LLM), matching the existing 2026-07-18 precedent / Stop and ask | Fall back to a deterministic single scene-wide cue | Bumping retries further just spends more wall-clock time on a call that's already proven unreliable this run; a single scene-wide cue is an already-human-approved remedy for this identical failure mode, not a new judgment call. Added `_single_scene_wide_cue_agent_call()` to `run_full_novel.py`: after the normal retry loop exhausts, one final Stage 09 call uses a hand-built `agent_call` returning one cue (`cue001`) spanning every beat, with `mood_tags` drawn from the union of the beats' own already-valid mood tags (guaranteed within Stage 09's `allowed_mood_tags`) - bypasses the LLM for cue boundaries entirely rather than keep retrying it | Claude (applying an already-established human precedent to a bulk-run orchestrator) |
| 2026-07-22 | animal_farm_ch1_2026_07_21 | 14_anime_style_conversion (new stage) | Mid-bulk-run, author asked "where is the anime pipeline? We decided to use AnimeGANv2 instead of pixel art, right?" No record of this decision existed anywhere - checked `ARCHITECTURE.md`, `DECISIONS_LOG.md`, `CLAUDE.md`, and Claude's own project memory (empty). Re-examined the git commit literally titled "anime style" (2026-07-18): its actual diff was confirmed to be only a `pixel_art_spec.yaml` `downscale_factor` change (160x90→320x180), unrelated to any GAN/anime model | Assume the decision happened elsewhere and proceed / Ask the author to confirm before building anything | Asked the author to confirm | Building a new ML-model-backed stage on an assumed-but-unverified prior decision risked significant wasted work if the memory was mistaken; the author confirmed they wanted it added as a new decision starting now | rpprabha1@gmail.com |
| 2026-07-22 | animal_farm_ch1_2026_07_21 | 14_anime_style_conversion (new stage) | Author confirmed wanting AnimeGANv2 added now. This machine is CPU-only (`torch.cuda.is_available()` is `False` - same hardware constraint already documented for sd-turbo). Measured real single-frame inference time before committing to samples: 0.38s/frame at 512x288 up to 4.6s/frame at 1920x1080 | Guess a resolution/style and commit directly / Render real comparison samples first, matching the same process already used for Stage 13's pixel-art technique | Render real comparison samples first | Matches this repo's own established discipline (Stage 13's 3-round real-sample review) rather than picking blind; author explicitly asked to see all outputs and choose. Cloned `bryandlee/animegan2-pytorch` (MIT-licensed) - it ships 4 pretrained checkpoints (`celeba_distill`, `face_paint_512_v1`, `face_paint_512_v2`, `paprika`). Rendered all 4 as real ~18s samples at 960px width from the real `ch1_sc1` `final.mp4` (not the full 12.8min chapter - full-length at native 30fps would have taken many hours per sample) | Claude (research + sample rendering), rpprabha1@gmail.com (chose `paprika`) |
| 2026-07-22 | animal_farm_ch1_2026_07_21 | 14_anime_style_conversion (new stage) | Testing `paprika` on a real text-card frame (no matched stock footage, plain drawn text over a solid background) showed it badly blurs the text toward illegibility - the model is trained on photographic/portrait content, not flat graphic/text content. Roughly a third of this run's beats are text-card fallbacks | Mask the style to real-footage clips only, leaving text cards crisp / Apply the style uniformly, accepting the legibility tradeoff | Apply uniformly | Shown a real before/after comparison frame first (not just described); author explicitly chose uniform application knowing the tradeoff rather than have the pipeline decide unilaterally which clips are "worth" styling | rpprabha1@gmail.com |
| 2026-07-22 | animal_farm_ch1_2026_07_21 | 14_anime_style_conversion (new stage) | Full chapter-length video (771s, 30fps = ~23,130 frames) at native frame rate would take many hours of CPU-only inference per chapter even at the fastest tested resolution - not tractable across 10 chapters at the 960px width the author already approved in the samples | Reduce resolution below what was approved / Reduce the stylization frame rate instead, holding each styled frame across several output frames | Reduce stylization frame rate (`stylize_fps: 6`, held back up to `output_fps: 30`) | Preserves the exact visual quality (960px, `paprika`) the author already reviewed and approved, rather than quietly serving a lower-fidelity result under time pressure; this pipeline's content (Ken Burns holds, static text cards) has little true motion, so a reduced stylization rate is expected to look effectively identical, and mirrors real "limited animation" production practice rather than being purely an expedient shortcut | Claude (config choice, flagged to author with reasoning) |
| 2026-07-22 | animal_farm_ch1_2026_07_21 | (run-wide) | Author reviewed the complete real chapter-1 render and flagged 7 concrete quality issues: (1) text-card beats instead of footage, (2) robotic/AI-ish TTS, (3) poor narration/visual sync, (4) weak clip search, (5) no real background music, (6) sub-HD anime output needing upscale, (7) overall visual flatness. Also killed the in-flight Stage 14 conversion of the old chapter-1 video (~3h remaining) since the base video would be invalidated by these improvements | Various per-item (see ARCHITECTURE.md change log 2026-07-23 for the full remedy list) | All 7 accepted as a batch; per-item choices: kill stale conversion (recommended, accepted); Wikimedia Commons + Archive.org both added (author: "Yes, add both"); music via Jamendo API after live verification that Pixabay's API has no music endpoint (author initially asked "why not use pixabay api for music" - answered with a real API probe, not just the repo's old note) | Remedies chosen against real measurements and samples: Kokoro voice picked from 4 real synthesized samples (bm_george), Real-ESRGAN timed for real on the MX230 (~1.0s/frame batch), Archive.org guardrails learned from real API probing | rpprabha1@gmail.com |
| 2026-07-23 | animal_farm_ch1_2026_07_21 | 08_timeline_builder | Crossfade default (part of the polish batch) would have made ~32 x 0.75s of transition time-borrowing accumulate into ~24s of freeze-frame tail padding via match_duration() - caught by reading the assembly code before running, not after a bad render | Accept freeze tail / over-provision each beat's visual window by its outgoing transition duration | Over-provision per beat | Keeps the post-crossfade video matching the audio track exactly instead of padding the tail; mechanical arithmetic, not judgment | Claude (design decision within the approved polish batch) |
| 2026-07-23 | animal_farm_ch1_2026_07_21 | 02_beat_extraction | `mood_tag_outside_vocabulary` recurred a 4th time - and much worse (11 invented words: distracted/drunk/foolish/gentle/joyful/majestic/motherly/old/profound/serious/varied) on the first run with the new `search_query` field, whose "concrete descriptive words" instruction appears to have bled into mood-tag selection. Two vocabulary expansions and a hardened prompt had not converged | Retry generation again / Expand vocabulary again / Strict code-level enforcement (drop invalid tags, backfill empty beats with the scene's most common valid tag, block only if nothing valid remains) | Strict code-level enforcement | Applies the author's standing "strict python condition, not prompt fixes" direction (set 2026-07-22 for the duplicate-visual issue) to this recurring failure class: mood tags only steer music search, so noise words carry no signal worth blocking a 20-minute regeneration over; the backfill is a mechanical statistic, not a judgment call; the degenerate all-invalid case still routes NEEDS_INPUT. Dropped tags are itemized in the stage summary so nothing is silent | Claude (applying the author's standing strict-code policy) |
| 2026-07-23 | animal_farm_ch1_2026_07_21 | 06_fallback_generation | Reviewing the real rendered chapter mid-batch, author flagged: "I still see images with just narration text in them. This is a bug and needs to be removed" - the fallback text card displays the same visual_description text that Kokoro TTS is simultaneously speaking aloud, reading as a broken slideshow | Reduce fallback frequency further (more/better footage search) / Remove the on-screen text, keep an animated visual / Generate real images via a diffusion model again | Remove the on-screen text, keep an animated Ken-Burns mood-colored gradient | The redundant text-on-screen was the actual defect, independent of how many beats fall back (some beats - e.g. a philosophical talking-pig speech - will never have real matching stock footage regardless of search quality); re-attempting sd-turbo wasn't pursued given 6 prior real OOM failures on this machine with no evidence the resource ceiling has changed | rpprabha1@gmail.com |
| 2026-07-23 | animal_farm_ch1_2026_07_21 | 06_fallback_generation | Re-running ch1 to verify the mood-visual fix, Stage 6 reported "COMPLETE...Generated 25 fallback asset(s)" but frame-extraction showed the OLD text card still - `if not video_path.exists(): render(...)` silently reused a cached clip from before the fix, since the beat_id filename hadn't changed | Manually clear the cache each time (fragile, easy to forget for the other 10 chapters) / Add cache-clearing to the orchestrator, matching the existing narration/music cache fix | Add cache-clearing to the orchestrator | Same root cause already fixed once for Stage 9's narration/music caches - Stage 06's generated_videos cache had the identical latent risk and was simply not caught until this specific re-run exercised it | Claude (applying the same established fix pattern) |
| 2026-07-23 | animal_farm_ch1_2026_07_21 | 02_beat_extraction | Previously-diagnosed but unfixed issue: the agent assigned the identical `search_query` ("pig speaking barn") to ~18 beats spanning genuinely distinct moments of Major's speech, starving Stage 03's search of any real per-beat specificity. Author's explicit instruction: "do not fix in prompt.. use the correct best practice for strict rules" | Prompt instruction telling the agent to vary search_query wording / Strict deterministic code: mechanically rewrite any beat's query that's shared with another beat in the same scene, using the same keyword-extraction fallback Stage 03 already has | Strict deterministic code | Matches the author's standing policy already applied to the duplicate-visual-beat and mood-tag-vocabulary issues above - a recurring model-reliability failure gets a code guard, not another prompt instruction. `extract_search_terms()` moved to `shared/text/` (Golden Rule 2: no stage-to-stage imports) so both Stage 02 and Stage 03 can use it; `_diversify_repeated_search_queries()` added to Stage 02, wired into `main()` right after the duplicate-visual-beat merge, with its count reported in the stage summary. 120/120 tests pass | Claude (applying the author's standing strict-code policy) |
| 2026-07-23 | animal_farm_ch1_2026_07_21 | 03_candidate_fetch | The just-applied search_query diversification fix (row above) caused a real Wikimedia search to return an actual hit for the first time this session, which immediately crashed the whole scene: `candidates.schema.json`'s `source` enum requires `"wikimedia_commons"`, but `WikimediaCommonsSource.name` was `"wikimedia"` - a latent naming bug, invisible until a real Wikimedia candidate actually reached schema validation | Loosen the schema to also accept "wikimedia" / Rename the source class attribute to match the schema's existing canonical name | Rename the source's `name` to `"wikimedia_commons"` | The schema's enum already matches `LICENSES.md`'s row name ("Wikimedia Commons") and CLAUDE.md's source-policy wording - the source implementation was the thing out of step, not the schema/docs; also added `shared/tests/test_wikimedia_source.py` (this source had zero prior test coverage) so a mismatch like this fails a test run instead of only a live pipeline run | Claude (root-cause fix, no live-run guessing) |
| 2026-07-23 | animal_farm_ch1_2026_07_21 | 09_audio_production | Same ch1 rerun, next stage after the wikimedia fix: `resolve_music_stage()` hard-crashed with `hitl_decisions references track_ref 'jamendo_682351' not among cue 'cue001''s candidates` - Stage 09 regenerates its entire cue-sheet (fresh LLM call) and re-runs the live Jamendo search on every single invocation with no caching, so a track_ref offered on one attempt legitimately wasn't among the next attempt's freshly re-searched candidates for the same cue_id | Patch the retry loop to drop the stale decision and hope the next attempt converges (symptom-level) / Root-cause fix: freeze the cue-sheet once it first validates and cache the live music search, so repeat calls in one resolution sequence can't drift | Root-cause fix (freeze + cache), keeping the drop-and-retry loop only as a secondary safety net | A pure retry-and-hope patch would still be racing the same two live non-determinism sources on every attempt; freezing the already-validated cue-sheet and caching identical-query search results removes the actual cause instead of papering over its symptom - matches the author's standing strict-code policy applied to a genuinely new failure mode (never seen with the synthetic GeneratedMusicSource placeholder). Added `tests/test_run_full_novel.py` (this orchestrator had zero prior test coverage) | Claude (root-cause fix) |
