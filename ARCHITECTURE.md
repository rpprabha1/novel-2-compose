# ARCHITECTURE.md — Living Architecture, Gates, and Change Log

This document is the detailed architectural companion to `CLAUDE.md`. `CLAUDE.md` sets the non-negotiable rules; this file describes *how* the system is built to satisfy them, and is expected to change over time (see Change Log at the bottom — every change here is logged, per Rule 9).

---

## 1. System overview

The pipeline turns one chapter/scene of prose into a rendered video cut. It is a strict linear sequence of 14 stages, each independently runnable and testable, coordinated hub-and-spoke by Stage 00. No stage talks to another stage directly.

```
 manuscript text
       │
       ▼
[01 manuscript_ingestion]  CODE
       │  scenes/chapters split by explicit markers
       ▼
[02 beat_extraction]       AGENT
       │  beats.json — ordered list of filmable visual beats
       ▼
[03 candidate_fetch]       CODE
       │  candidates.json — raw hits per beat from FootageSource APIs
       ▼
[04 clip_reranking]        CODE
       │  CLIP-embedding cosine score, threshold routing
       ▼
       ├─ above threshold ──────────────┐
       │                                ▼
       │                    [05 retrieval_verification]  CODE + HITL
       │                          frame-sample re-score, human tie-break
       │                                │
       └─ below threshold ──────┐       │
                                 ▼       ▼
                      [06 fallback_generation]  CODE by default (HYBRID/AGENT opt-in, 2026-07-18)
                         image-gen prompt (agent) + render (code)
                                 │       │
                                 └───┬───┘
                                     ▼
                        assets_manifest.json (winning asset per beat,
                        license + attribution captured at fetch time)
                                     │
                                     ▼
                     [07 editorial_direction]  CODE by default (AGENT opt-in, 2026-07-18)
                        edit_plan.json — shot division, pacing, transitions
                                     │
                                     ▼
                     [08 timeline_builder]  CODE
                        timeline.json — materialized, validated
                                     │
                                     ▼
                     [09 audio_production]  HYBRID
                        audio_plan.json (agent) → audio_mix.json + stems (code)
                                     │
                                     ▼
                     [10 human_review_gate]  CODE + human
                        contact-sheet HTML, human sign-off
                                     │
                                     ▼
                     [11 assembly_render]  CODE
                        final.mp4 (ffmpeg, fully deterministic)
                                     │
                                     ▼
                     [12 qa_attribution]  CODE
                        schema validation, license/attribution completeness,
                        duration + loudness checks, CREDITS.md
                                     │
                                     ▼
                     [13 pixel_art_conversion]  CODE
                        final_pixel_art.mp4 (nearest-neighbor grid + dithered
                        palette restyle, ffmpeg, fully deterministic)
                                     │
                                     ▼
                     [14 anime_style_conversion]  CODE
                        final_anime.mp4 (AnimeGANv2 GAN restyle at a reduced
                        stylize_fps, ffmpeg extract/reassemble, deterministic
                        given a fixed checkpoint)
```

All arrows are Coordinator-mediated Task Envelopes / Stage Responses (see `SCHEMAS.md`), never direct stage-to-stage calls. `00_coordinator` sits beside this chain, not "before" it — it invokes every stage, holds run-level state, and is the only component that talks to the human.

---

## 2. Per-stage detail

Stages 07 and 09 are already specified in full in `CLAUDE.md` §4 — not repeated here. The remaining stages:

**00_coordinator (AGENT orchestration + CODE core).** The code core owns: envelope construction, stage invocation order, schema validation of every Stage Response against `expected_output_schema`, gate enforcement (refuses Stage N+1 without `outputs/APPROVED.md` on Stage N), and append-only logging to `shared/runs/<run_id>/coordinator_log.jsonl`. The agent layer owns: turning a `NEEDS_INPUT` or `ESCALATE` Stage Response into a human-readable question with options and tradeoffs, and batching multiple pending questions from one stage into a single presentation. The agent never decides sequencing or gate pass/fail — that's code, checked against `APPROVED.md` presence, not agent judgment.

**01_manuscript_ingestion (CODE).** Input: raw manuscript text file + an explicit marker convention (e.g. `## Chapter` / `### Scene` headers, configurable, not inferred) declared in `run_config.yaml`. Output: one or more scene text files, encoding-normalized (UTF-8), with scene boundaries and any inline metadata (POV character, chapter number) preserved as plain fields. No interpretation of content — pure text splitting.

**02_beat_extraction (AGENT).** Input: one scene's text + `run_config` (tone, pacing). Output: `beats.json`, an ordered list of filmable visual beats, each with a text excerpt reference, a plain-language visual description (what a camera would see), estimated on-screen duration, and mood/intensity tags drawn from `config/audio_spec.yaml`'s vocabulary. This is the first and most judgment-heavy step: deciding what counts as one filmable unit versus two, and what's genuinely visual versus interior narration that has no direct visual analog (those get flagged, not invented).

**03_candidate_fetch (CODE) — RETIRED 2026-07-24 (downloader-lane cutover; code kept, no longer invoked).** Footage now comes from the source-free downloader lane (`01_1_downloader` → `shared/downloader_manifest.py` → `01_2_scene_scoring` → `shared/downloader_assets.py`), not this stock-API lane. Historical behavior: Input: `beats.json`. For each beat, queries every enabled `FootageSource` (see `shared/sources/`) with search terms derived mechanically from the beat's visual description (no agent involved in query construction — that's a deterministic keyword/phrase extraction). Output: `candidates.json`, raw API hits per beat, each candidate tagged with source, license, and a stub for the not-yet-computed similarity score. Caching and rate limiting live here.

**04_clip_reranking (CODE) — RETIRED 2026-07-24 (downloader-lane cutover; code kept, no longer invoked; its CLIP-scoring role is now `01_2_scene_scoring`'s).** Historical behavior: Input: `candidates.json`. Computes CLIP embedding similarity between each candidate's thumbnail/keyframe and the beat's visual description, via `shared/embeddings/`. Routes each beat: above `thresholds.yaml`'s similarity cutoff → `05_retrieval_verification`; below → `06_fallback_generation`; within the configured margin of the cutoff → flagged `retrievable: low` for human tie-break in 05.

**05_retrieval_verification (CODE + HITL) — RETIRED 2026-07-24 (downloader-lane cutover; code kept, no longer invoked; the downloader lane already downloads the real clip and `01_2_scene_scoring` frame-scores it).** Historical behavior: Downloads the top-k candidates per beat, samples frames across the actual clip (not just the thumbnail), re-scores. Beats whose top candidates are within the "close" margin from `thresholds.yaml` are batched and presented to the human via the Coordinator (Rule 10) rather than auto-selected. Output: winning asset per beat with confidence, or a `FALLBACK_ROUTED` handoff to 06 for beats that fail verification.

**06_fallback_generation — RETIRED / DISABLED 2026-07-24 (downloader-lane cutover; `src/run.py` is now a no-op that generates nothing, folder kept per "retire, keep folders").** The synthetic fallback is removed per the author's "strictly remove the backup option … not needed at all"; beats the downloader lane can't cover simply contribute no asset. Historical behavior (CODE by default, HYBRID/AGENT+diffusion opt-in — see change log 2026-07-18): For beats with no acceptable retrieved asset. Default CODE path: a plain ffmpeg text card (wrapped `visual_description` over a solid background, `shared/media/generate_text_card()`) — no agent, no diffusion model, no RAM/disk risk. Original HYBRID path (still implemented, reachable via explicit `agent_call`): agent half writes an image-generation prompt from the beat's visual description, tone, and mood tags — never inventing plot detail beyond what's in the beat; code half calls the sd-turbo generation backend and applies Ken Burns zoompan to turn a still into video-length footage. Either path's output feeds into the same `assets_manifest.json` as 05's winners, tagged with source `generated` (no attribution needed, but flagged distinctly for QA).

**07_2_narration_shot_mapping (CODE, added 2026-07-24 — see change log).** Runs after `09_audio_production`. For each beat it takes that beat's top-ranked downloader clips (from `01_2_scene_scoring`), tiles the beat's narration duration into ~shot-length windows (default = the active pacing preset's `hold_duration_s.max`), and **physically extracts** each window as its own short `.mp4` (via `shared/media.trim_clip`), advancing distinct positions across the clips so a long clip yields real visual progression and multiple clips alternate. A clip whose extraction fails (undecodable input) is dropped for that beat and coverage continues from the rest. Outputs `shot_map.json` (the explicit narration→shot mapping), a source-free `assets_manifest.json` (each extracted shot an asset), and `edit_plan.json` — superseding `07_editorial_direction` for the downloader lane. This makes the shot cutting explicit and file-level, rather than leaving it as implicit window references that `08_timeline_builder` used to compute at timeline time.

**08_timeline_builder (CODE).** Input: approved `edit_plan.json` + `assets_manifest.json`. Pure transformation: resolves every shot's in/out offsets and transition into a single `timeline.json` (absolute timecodes, file references, transition parameters), and validates it against `shared/schemas/timeline.schema.json`. No creative decisions — those were already made and approved in 07.

**10_human_review_gate (CODE + human).** Generates a contact-sheet HTML (thumbnail grid + timeline scrub markers + audio cue markers) from `timeline.json` and `audio_mix.json`. The human reviews and either approves (writes `outputs/APPROVED.md`) or requests changes, which route back through the Coordinator to the relevant upstream stage. No agent involvement — this stage's only job is rendering a reviewable artifact.

**11_assembly_render (CODE).** Input: approved `timeline.json` + `audio_mix.json`. Deterministic ffmpeg pipeline: trim, concat, color grade (if specified), mux audio, output `final.mp4`. No decisions made here that weren't already fixed upstream.

**12_qa_attribution (CODE).** Validates every artifact against its schema, checks every fetched asset in the run manifest has a complete license/attribution record (CC-BY assets without a creator record fail QA), checks final duration against the beat plan's target (within `thresholds.yaml`'s tolerance) and loudness against `audio_spec.yaml`'s LUFS target. Emits `CREDITS.md` and a QA report. A failing QA report blocks the run from being marked done — it does not block re-running upstream stages.

**13_pixel_art_conversion (CODE, added 2026-07-18 — see change log).** Input: the Stage 12-approved `final.mp4`. Restyles it into a retro pixel-art look via a deterministic ffmpeg filter chain (`shared/media/pixel_art.py`): area-averaged downscale to a pixel grid sized proportionally to the source resolution (`config/pixel_art_spec.yaml`'s `downscale_factor`), palette reduction via `palettegen`/`paletteuse` with ordered (bayer) dithering, an edgedetect-based dark outline overlay on real object boundaries (`edge_low`/`edge_high` thresholds, luma-only so color survives), nearest-neighbor upscale back to source resolution. Output is `final_pixel_art.mp4`, written alongside (never replacing) `final.mp4`. No agent involvement and no creative judgment left to make here — the human picked this specific technique across two rounds of reviewing real rendered samples of the real video before/while this stage was built (first the palette-dither look over two plainer alternatives, then the area-avg+outline fix over two rejected variants — see the entry directly below); this stage only mechanizes that already-made choice. Numeric pass criterion: output duration within `thresholds.yaml`'s `pixel_art.duration_tolerance_pct` (1%) of the source, since this stage only re-filters frames and never re-times anything.

**14_anime_style_conversion (CODE, added 2026-07-23 — see change log).** Input: the Stage 12-approved `final.mp4`. Restyles it into an anime look via AnimeGANv2, a pretrained GAN vendored under `shared/models/animegan/` (MIT-licensed; architecture in `model.py`, checkpoint `paprika.pt`), run per-frame through `shared/generation/animegan_backend.py`'s `stylize_video()`. Frames are extracted at a reduced rate (`config/anime_style_spec.yaml`'s `stylize_fps`, not the source's native frame rate) since single-frame CPU inference is expensive (~2-3s/frame at 960px on the dev machine); each styled frame is then held across several output frames at `output_fps` to restore standard playback smoothness — a real "limited/on-twos animation" convention, not merely an expedient, and one this pipeline's mostly-static content (Ken Burns holds, static text cards) tolerates well. Output is `final_anime.mp4`, written alongside (never replacing) `final.mp4`/`final_pixel_art.mp4`. No agent involvement — the human picked the checkpoint after reviewing 4 real rendered samples (`celeba_distill`, `face_paint_512_v1`, `face_paint_512_v2`, `paprika`) from the real `ch1_sc1` `final.mp4`, and separately decided (after being shown the effect) to apply the style uniformly despite it visibly blurring text-card beats toward illegibility, rather than mask fallback-origin clips out of the stylization pass. Numeric pass criterion: output duration within `thresholds.yaml`'s `anime_style.duration_tolerance_pct` (2% — looser than pixel_art's 1% since this stage re-times through a reduced-then-restored frame rate, not a straight re-filter) of the source.

---

## 3. Gate 0 — manual coverage test (precedes ALL implementation)

**Purpose.** Before writing a single line of pipeline code, prove that the Source Policy (CLAUDE.md §0 — Pexels, Pixabay, Mixkit, Coverr, Archive.org public domain, Wikimedia Commons, NASA) can plausibly cover a real scene's visual beats at a workable rate. If it can't, the whole retrieval-based design (stages 03-05) needs to pivot toward heavier reliance on the fallback-generation lane before any of that code gets built. This is a coverage feasibility test, not a pipeline dry run — no code is exercised.

**Procedure.**
1. Take the standing fixture scene at `shared/fixtures/sample_scene.txt` (or a real manuscript excerpt, once available — the procedure is identical either way).
2. By hand, decompose it into 8-12 visual beats — exactly the judgment call Stage 02's agent will later automate. Write each beat as a one-sentence "what would the camera see" description.
3. For each beat, manually search Pexels, Pixabay, Mixkit, Archive.org (public domain filter on), and Wikimedia Commons via their web UIs (no API keys required for this manual pass). Try 1-3 search-term variations per beat.
4. For each beat, record: search terms tried, best candidate found (title/URL/source), its license, and a relevance judgment — `good` (would plausibly be selected by a real similarity threshold), `marginal` (fallback-eligible but not a clean match), or `none` (nothing usable found).
5. Compute coverage: `(good + marginal) / total beats`, and separately, `good / total beats`.
6. Record everything in `docs/GATE0_RESULTS.md` and make a GO/PIVOT call against the criterion below.

**Numeric pass criterion** (seeded in `config/thresholds.yaml` as `gate0_min_coverage_pct: 70`):
- GO if `(good + marginal) / total beats >= 70%` **and** zero beats are `none` (every beat has at least a fallback-eligible candidate, since `none` beats reveal a gap not just in stock coverage but in the fallback-generation lane's assumptions too).
- Otherwise PIVOT: stop, do not proceed to stage implementation, escalate to the human with the specific failing beats and a recommendation (e.g. lean harder on fallback-generation, reconsider genre/scene selection for early testing, or add a source).
- A criterion failing twice after adjustments escalates per CLAUDE.md Rule 6 — never silently lowered.

**Status:** This session, only the procedure and the `docs/GATE0_RESULTS.md` template are being created. Running Gate 0 to an actual GO/PIVOT decision is a follow-up step requiring a live manual search pass — see that file.

---

## 4. Change Log

| Date | What | Why | Approved by |
|---|---|---|---|
| 2026-07-14 | Initial creation of ARCHITECTURE.md, SCHEMAS.md, LICENSES.md, DECISIONS_LOG.md, and full repo scaffold (config/, shared/, stages/) | Bootstrap the project structure CLAUDE.md v2 already assumes exists; nothing existed yet beyond CLAUDE.md itself | rpprabha1@gmail.com |
| 2026-07-14 | Gate 0 executed against `shared/fixtures/sample_scene.txt`: 100% beat coverage (good+marginal), 0 dead-end beats — GO. See `docs/GATE0_RESULTS.md`. Rule 5 now unblocked; Stage 01 implementation starting. | Required before any pipeline code per CLAUDE.md rule 5 | rpprabha1@gmail.com |
| 2026-07-14 | Added `scenes_manifest.schema.json` + SCHEMAS.md entry for 01_manuscript_ingestion's output; this contract wasn't anticipated in the initial schema set | Discovered while implementing Stage 01 — 01's output needed a formal contract before 02 can consume it | rpprabha1@gmail.com |
| 2026-07-14 | Added `shared/agents/` (Ollama backend + config loader) and `config/agents.yaml`. AGENT/HYBRID stages (02, 06, 07, 09) run against a local Ollama model (default `llama3.2:3b`, sized to the dev machine's 2GB-VRAM GPU / ~8GB RAM) rather than a hosted LLM API. | Human decision: use local open-source models via Ollama, sized appropriately to available hardware, instead of a paid hosted API | rpprabha1@gmail.com |
| 2026-07-14 | Implemented `shared/sources/pexels.py` + `pixabay.py` (the two `FootageSource` adapters with API keys in progress) and added `shared/manifest.py`, a small helper every fetching stage (03, and later 05/06/09) uses to append/de-dupe `shared/runs/<run_id>/manifest.json` entries — this cross-stage helper wasn't anticipated in the initial scaffold. | Discovered while implementing Stage 03 — manifest-append logic is identical across every fetching stage and belongs in `shared/`, not duplicated per stage | rpprabha1@gmail.com |
| 2026-07-14 | Implemented `shared/embeddings/` (HuggingFace `transformers` CLIP, `openai/clip-vit-base-patch32`, config in new `config/embeddings.yaml`). Extended `candidates.schema.json` with an optional per-beat `routing` object (`route`/`best_score`/`retrievable`) written by Stage 04; also widened `similarity_score`'s range to `[-1, 1]` (cosine similarity can be negative; the original `[0, 1]` bound was wrong). | Discovered while implementing Stage 04 — routing decision needed a schema home, and the similarity_score bound was an error in the initial schema pass | rpprabha1@gmail.com |
| 2026-07-14 | Added optional `download_url` to `candidates.schema.json` and to `FootageCandidate`/`PexelsSource`/`PixabaySource`. Stage 03 had only captured each candidate's human-facing page URL, not a direct downloadable file URL — Stage 05 needs the latter to actually download video for frame-sampling verification. | Discovered while starting Stage 05 — a real gap in Stage 03/04's output, not anticipated when those stages were built | rpprabha1@gmail.com |
| 2026-07-14 | Added `shared/media/` (ffmpeg/ffprobe CLI wrappers: `probe_duration_s`, `extract_frames`). Used by Stage 05 for frame sampling now; will be reused by Stage 06's Ken Burns zoompan and Stage 11's assembly render rather than duplicating ffmpeg subprocess calls per stage. | Anticipated reuse across every stage that touches actual video files | rpprabha1@gmail.com |
| 2026-07-14 | Added `shared/generation/` (HuggingFace `diffusers`, `stabilityai/sd-turbo`, config in new `config/image_gen.yaml`) for Stage 06's code half, `shared/media.ken_burns_zoompan()` for turning a still into video-length footage, `config/visual_style.yaml` (tone -> image style modifiers + negative-prompt defaults, same pattern as `audio_spec.yaml`'s tone->music-tag map), and `fallback_prompt.schema.json` for the agent-half output. Human decision: local diffusion on CPU (sd-turbo), consistent with the Ollama/CLIP local-model choices already made — smoke-tested at ~35-60s/image, quality judged good enough for the fallback lane. | Human decision, matching the established "local models sized to this hardware" pattern from `config/agents.yaml`/`config/embeddings.yaml` | rpprabha1@gmail.com |
| 2026-07-14 | Fixed `edit_plan.schema.json`: `shots` had `maxItems: 3`, which made CLAUDE.md's own stated behavior (">3 shots is a HITL trigger") schema-invalid to represent. Raised to `maxItems: 12` (a sanity ceiling only); the >3 HITL check is enforced in Stage 07's code, not the schema. | Caught before Stage 07 implementation began — a real authoring error in the initial schema pass | rpprabha1@gmail.com |
| 2026-07-14 | Added `config/thresholds.yaml`'s `editorial.hold_duration_clamp_tolerance_pct` (10%): a `hold_duration_s` within this tolerance of the active pacing preset's range is coerced to the nearest bound in Stage 07's code instead of blocking on `NEEDS_INPUT`. The range itself is unchanged — this is coercion of a near-miss, not a lowered threshold. | Human decision after the real run showed `llama3.2:3b` has a small, systematic numeric-precision limitation on hold_duration_s (not a prompt-wording issue); see `DECISIONS_LOG.md` | rpprabha1@gmail.com |
| 2026-07-14 | Clarified `edit_plan.schema.json`'s shot field semantics: `[in_s, out_s]` is the usable source window (an availability ceiling), `hold_duration_s` is the authoritative on-screen duration and must fit inside that window (`hold_duration_s <= out_s - in_s`) — `08_timeline_builder` trims to `[in_s, in_s + hold_duration_s]`, never to `out_s` directly. Added a Stage 07 code-side check enforcing this, since the real edit_plan.json had `out_s` values looser than `hold_duration_s` and the schema didn't previously say which was authoritative. | Caught while starting Stage 08 — the ambiguity would have forced 08 to guess | rpprabha1@gmail.com |
| 2026-07-14 | Corrected `LICENSES.md`: Pixabay Music was listed as having a public API ("music endpoint") — this was never actually verified and is false. Pixabay's documented public API covers images and videos only. | Caught while starting Stage 09 — would have built a `MusicSource` adapter against an API that doesn't exist | rpprabha1@gmail.com |
| 2026-07-14 | Added `shared/sources/music_base.py` (`MusicSource`/`MusicCandidate`, mirrors `FootageSource`) and `shared/sources/manual_music.py` (`ManualMusicSource`, a human-curated candidate list — the only real `MusicSource` implementation until an approved source gets a public API). Added `shared/generation/tts_backend.py` (local `piper-tts`, `config/tts.yaml`, model in `shared/models/piper/`) and `shared/media/audio_mix.py` (crossfade, ducking, narration overlay, loudness normalization — smoke-tested against synthetic sine-wave audio, measured LUFS landed within 0.11 of target). Added `music_cue_intent.schema.json` for the agent-half output. | Human decisions: local `piper-tts` for narration (same "fit this hardware" pattern as Ollama/CLIP/sd-turbo); manual music sourcing given no approved source has a real search API | rpprabha1@gmail.com |
| 2026-07-15 | Fixed `shared/manifest.py`'s `append_manifest_entries()`: it only de-duplicated within a single call's own entries, not against what was already on disk. A real run's `manifest.json` accumulated true duplicates (the same `entry_id` 2-3 times) because Stage 05/06/09 were each re-run multiple times during debugging/recovery this session — `CREDITS.md` showed the same asset three times. Fixed to de-dupe by `entry_id` against existing entries (last write wins). This surfaced a second, subtler bug: Stage 05's manifest entries never carried `source_url` (only Stage 03's did), so once de-duped, "last write wins" clobbered the more complete Stage 03 entry with Stage 05's URL-less one for every actually-selected asset. Fixed Stage 05 to carry the candidate's `url` through into its manifest entries. | Caught while reviewing Stage 12's real `CREDITS.md` output — duplicated and then incomplete attribution would have shipped silently otherwise | rpprabha1@gmail.com |
| 2026-07-15 | Added `config/editorial_vocab.yaml`'s `transition_durations_s` (hard-cut/match-cut-suggestion: 0.0, crossfade: 0.75, dip-to-black: 0.5). `08_timeline_builder` had hardcoded every `transition_out.duration_s` to `0.0` regardless of type — a "crossfade" with zero duration is indistinguishable from a hard-cut. Caught before implementing Stage 11, which would have had nothing to actually blend. | Caught while starting Stage 11 — a real gap that would have silently produced instant cuts labeled "crossfade" | rpprabha1@gmail.com |
| 2026-07-15 | `08_timeline_builder` now optionally reads `inputs/audio_mix.json` (backward compatible — behaves exactly as before if absent). When present, each beat's `hold_duration_s` is extended (never shortened, multi-shot beats scaled proportionally) to cover its narration duration, since audio timing is authoritative (see the 09_audio_production entry below). A beat whose asset can't cover its narration even from `in_s=0` is routed `FALLBACK_ROUTED` (reason `asset_too_short_for_narration`) rather than clipped/looped — the same "no match is a routed outcome" principle Stage 04/05 already follow, and still a mechanical/threshold routing decision, not creative judgment, so this stays CODE. | Closes the follow-up opened by the audio-drives-timing change below — resolves the real scene's stale `timeline.json` | rpprabha1@gmail.com |
| 2026-07-15 | Fixed `shared/media/audio_mix.py`'s `normalize_loudness()`: single-pass `loudnorm` measured 1.44 LU off the -16.0 target on a real mix (outside `audio_spec.yaml`'s `tolerance_lu: 1.0` — a genuine numeric-criterion failure, not a rounding note). Switched to standard two-pass loudnorm (analysis pass measures real input stats, apply pass uses `linear=true` with those measured values); re-verified against synthetic audio at 0.05 LU off target. | Rule 6: a failing numeric criterion doesn't get waved through | rpprabha1@gmail.com |
| 2026-07-15 | **Architecture change: audio timing now drives video timing, not the reverse.** Real run exposed that Stage 07's visual `hold_duration_s` (2.75-3.75s/beat, picked with zero awareness of narration) was wildly shorter than the actual time to read each beat's full source paragraph aloud (8-15s/beat) — narration stems placed at visual beat-start times overlapped 3-5 deep into an unusable mix. Fixed: `09_audio_production` no longer reads `edit_plan.json` at all (only `beats.json`'s order + the scene's source text); narration stems are now placed sequentially by actual synthesized duration, and the resulting (longer) sum becomes `audio_mix.json`'s new `total_duration_s` field (schema updated). **Consequence, not yet implemented:** `08_timeline_builder`'s `timeline.json` is now stale after 09 runs — each beat's visual hold must be regenerated to at least match its narration duration before `11_assembly_render` can use it. This is an open follow-up, not silently papered over. | Human decision: narration timing must be authoritative for a narrated-prose format; visual cuts follow audio, not the other way around | rpprabha1@gmail.com |
| 2026-07-17 | Fixed `run_config.schema.json`: `pacing`'s enum was `[slow-burn, standard, fast-cut]`, missing the `dynamic` preset added to `config/editorial_vocab.yaml` the same day (see the multi-angle entry directly below) — a real `run_config.yaml` setting `pacing: dynamic` would fail this schema if anything validates against it (nothing currently does, but the schema is the documented contract per `SCHEMAS.md`). Caught while assembling a real `run_config.yaml` to test the Pixabay API key end to end. | Caught before it could cause a silent validation gap - `SCHEMAS.md`/`CLAUDE.md` rule 9 requires the schema to match what's actually valid | rpprabha1@gmail.com |
| 2026-07-17 | **Architecture change: real multi-angle shot cutting instead of one long hold per beat.** Author feedback: every beat rendered as one continuous shot, which reads as static/boring — wanted a different shot (ideally a different real camera angle, not a synthetic reframe) roughly every 3-4s. Root cause: `05_retrieval_verification` already downloads and frame-verifies `top_k` candidates per beat but discarded all but the winning one before writing `assets_manifest.json`; `07_editorial_direction`'s prompt also defaulted to "one shot per beat" as the norm. Fixed across four files: (1) `config/thresholds.yaml` added `retrieval_verification.assets_per_beat` (default 3) so Stage 05 now retains that many ranked (`rank: 1..N`) verified candidates per beat instead of just the winner — no new fetching, these clips were already downloaded; (2) `assets_manifest.schema.json` widened to allow multiple asset entries per `beat_id`, distinguished by optional `rank` (absent = 1, fully backward compatible); (3) `edit_plan.schema.json`'s shot items gained an optional `asset_id` (which of the beat's available assets that specific shot draws from; omitted = the beat's primary/rank-1 asset, also backward compatible); (4) `config/editorial_vocab.yaml` added a `dynamic` pacing preset (`hold_duration_s: 3.0-4.0s`, `max_shots_per_beat: 4`). `07_editorial_direction` now receives each beat's full `available_assets` list and its `AGENT_PROMPT.md` instructs it to prefer distributing shots across *different* assets (real angle changes) rather than re-slicing one clip, which produces no visible change since `11_assembly_render` has no pan/zoom/reframe capability; the over-subdivision HITL check (`CLAUDE.md` rule 10 / `hitl_shot_subdivision_threshold`) was corrected to count shots *per resolved asset* rather than per beat, since a multi-angle beat can legitimately have more total shots than any single asset's share. `08_timeline_builder` now resolves `file_ref`/`duration_s` per shot (falling back to the beat's primary asset_id) instead of once per beat, including in the narration-reconciliation fit check. `11_assembly_render` required no change — it already builds `final.mp4` from each timeline clip's own `file_ref`. | Human decision (author): shots holding too long read as visually boring; alternating between real, already-verified camera angles every 3-4s is more dynamic than either longer holds or a synthetic Ken-Burns reframe of the same single clip | rpprabha1@gmail.com |
| 2026-07-18 | Fixed `shared/generation/image_backend.py`: `AutoPipelineForText2Image.from_pretrained()` had no `variant=` argument, so it downloaded sd-turbo's full fp32 checkpoint files (~5.05GB: 1.36GB text_encoder + 3.46GB unet + 0.33GB vae) instead of the repo's much smaller fp16 files (~2.58GB total, roughly half). Added `variant="fp16"` — this only changes which serialized checkpoint files are fetched; `torch_dtype=torch.float32` (unchanged) still controls the actual compute dtype after loading, so there's no precision/quality tradeoff, only a download-size one. | Caught on a real run: the fp32 default exhausted disk on a constrained dev machine mid-download (same "fit this hardware" reasoning already applied in config/image_gen.yaml, config/agents.yaml, config/embeddings.yaml, config/tts.yaml) | rpprabha1@gmail.com |
| 2026-07-18 | Fixed `09_audio_production`'s `allowed_mood_tags` computation: it was only `config/audio_spec.yaml`'s `tone_music_tags[tone]`, ignoring the run's own beats' `mood_tags` entirely — despite that file's own comment documenting the intended design as the *union* of the two. A beat legitimately tagged outside the tone's list (e.g. a "romantic" beat within an overall gothic-suspense scene) made every cue covering it schema-unsatisfiable no matter what the agent produced, since the agent is shown that beat's real `mood_tags` in its prompt but validated against a narrower set it was never told to stay within. Fixed to compute the union of `tone_music_tags[tone]` and the mood tags actually present across this run's beats (not the full global `mood_vocabulary` — only tags genuinely in play this run). Added a regression test (`test_cue_mood_tag_from_beats_own_tag_is_allowed`). | Caught on a real run: reproduced identically across two different Ollama models (`llama3.2:1b` and `llama3.2:3b`), which ruled out model capability and pointed to a structural bug instead of a reliability issue | rpprabha1@gmail.com |
| 2026-07-18 | Changed `shared/generation/image_backend.py`'s `torch_dtype` from `float32` to `float16` (keeping the already-fixed `variant="fp16"` file selection). Rationale: loading fp16-on-disk weights with `torch_dtype=float32` still upcasts each tensor to float32 during load, so peak memory briefly approaches the full-precision ~5GB footprint regardless of the smaller download — this is almost certainly why loading segfaulted/OOM'd repeatedly on a constrained (8.47GB RAM) dev machine even after the disk-side fix. Staying in float16 end-to-end should keep real memory use near the ~2.6GB file size instead. **Not confirmed working**: every real attempt this session, including after this change, ran on a machine already critically low on both RAM and disk (as low as 88MB disk, ~1.8GB RAM free) from the session's own preceding activity, and segfaulted immediately at 0% load progress — worse than pre-fix attempts that at least reached 84%. The change is logically sound and should be retested on a machine with real headroom before being trusted; kept in the codebase as the best available fix, not reverted, since the segfaults are attributable to the machine's momentary resource state, not evidence the change itself is wrong. | Attempted to unblock 06_fallback_generation for beats with no matched footage; genuinely inconclusive due to the test machine's resource state at the time, not a negative result | rpprabha1@gmail.com |
| 2026-07-18 | Fixed `11_assembly_render`'s normalized-clip cache: `dest = cache_dir / f"norm_{clip['shot_id']}.mp4"` was keyed only by `shot_id`, and `if not dest.exists(): normalize_clip(...)` silently skipped regeneration. Re-rendering the same `run_id` after the upstream `edit_plan.json`/`timeline.json` changed (e.g. the 07_editorial_direction reclassification below, which changed which asset/trim each `shot_id` maps to) reused stale cached clips from an *earlier* render whenever a shot_id happened to already exist in the cache dir — silently mixing correct, freshly-generated clips with leftover-wrong content/durations from a prior run into one `final.mp4`, with no error or warning anywhere. Caught for real: verified via checksum that `norm_ch1_sc1_b001_s1.mp4` and `norm_ch1_sc1_b001_s2.mp4` were byte-identical despite the current timeline.json declaring two different real source videos, and via ffprobe that 6 of 9 cached clips had durations matching an earlier render's edit_plan, not the current one. Fixed by folding `file_ref`/`source_in_s`/`source_out_s` into the cache key (`norm_<shot_id>_<sha256-prefix>.mp4`) so any change in a shot's actual source forces regeneration; unchanged shots still correctly reuse their cache. Added a regression test (`test_reused_shot_id_with_changed_source_is_not_stale_cached`). | Caught while re-rendering after the 07_editorial_direction fix below — the author reported still seeing long, repeated-looking shots in the "corrected" output, which led directly to finding this | rpprabha1@gmail.com |
| 2026-07-18 | **Architecture change: 07_editorial_direction reclassified AGENT → CODE by default (AGENT mode kept, opt-in).** On a real full pipeline run, the author noticed the final video kept holding on the same clip for the full multi-shot duration of a beat — e.g. `ch1_sc1_b001`'s two shots both used `pixabay_6129`, `ch1_sc1_b004`'s both used `pixabay_179212` — even though each beat had 2-3 different verified real assets available (the 2026-07-17 multi-angle plumbing). Root cause, confirmed across two different real runs and two different Ollama models (`llama3.2:1b` and `llama3.2:3b`): the editorial agent consistently failed to leverage `available_assets` correctly — hallucinating an `asset_id`, cross-contaminating one beat's asset onto another, or (most often) simply re-specifying the same primary asset for every shot despite `AGENT_PROMPT.md` instructing it to prefer different ones. Author decision: stop relying on the agent for shot-to-asset assignment entirely, at least for now — mechanically use every retained verified asset (already downloaded, no new fetching) as its own shot instead. Implemented as `_build_deterministic_edit_plan()` in `stages/07_editorial_direction/src/run.py`: for each beat, every asset at or above `min_viable_shot_length_s` (capped at the pacing preset's `max_shots_per_beat`) becomes one shot, `in_s=0`, `hold_duration_s` = the beat's `est_duration_s` divided across its shots and clamped into `[hold_min, hold_max]` then capped at that asset's own duration, `transition_out` always `hard-cut`. `main()`'s `agent_call` parameter now branches: `None` (the CLI/test default) → deterministic CODE path; an explicit `agent_call` → the original AGENT path is still fully implemented and reachable, unchanged, for later revival. All of Stage 07's existing HITL safety nets (`edit_plan_incomplete` vocabulary/range validation, `over_subdivided_shots`, `repeated_dramatic_transition`, `runtime_drift`) run unchanged against the deterministic output too, so human-in-the-loop coverage isn't reduced by removing the agent. `CLAUDE.md`'s stage-classification table and this file's stage diagram both updated to reflect CODE-by-default. `AGENT_PROMPT.md` kept as-is (not deleted) since AGENT mode remains a real, supported code path, just not the default. | Human decision (author): the agent proved unreliable at exactly the sub-task the 2026-07-17 multi-angle feature depended on, confirmed reproducible across models rather than a one-off fluke; mechanical code sidesteps that reliability ceiling entirely rather than continuing to chase better prompts/models | rpprabha1@gmail.com |
| 2026-07-18 | **Architecture change: new Stage 13, `13_pixel_art_conversion` (CODE), added after `12_qa_attribution`.** Author request: render the whole movie in a pixel-art aesthetic. Given this machine's repeatedly-demonstrated RAM/disk fragility (see the sd-turbo entries above), evaluated only options with no extra install/model-load risk: ffmpeg's native `pixelize` filter, plain nearest-neighbor downscale/upscale, a downscale + `palettegen`/`paletteuse` dithered-palette approach, and (researched but explicitly not run) `Pyxelate` (CPU-only ML library, ruled out for now purely on install-size risk against ~525MB free disk, not on merit) and two GUI/browser-only tools (PAC-Pixel-Art-Converter, collidingScopes' video-to-pixel-art) not scriptable for batch sampling. Rendered all 3 ffmpeg-only options as real ~15s samples from the real `final.mp4`, visually verified each via extracted/zoomed frames (plain nearest-neighbor and `pixelize` kept full color depth and read as "low-res" rather than "pixel art"; the dithered-palette version showed genuine retro color-speckle). Author reviewed and picked the dithered-palette version. Implemented as a new stage rather than a one-off script per Rule 1 (one stage = one folder) and Rule 9: `shared/media/pixel_art.py` (`apply_pixel_art_style()`), `shared/media/ffmpeg_utils.probe_resolution()` (new — no prior stage needed source resolution directly), `config/pixel_art_spec.yaml` (downscale factor, palette size, dither method/scale — all config-driven per Rule 11, grid size computed proportionally from the source's actual resolution rather than hardcoded), `config/thresholds.yaml`'s new `pixel_art.duration_tolerance_pct` (1%, since this stage only re-filters frames and never re-times). `CLAUDE.md` §2/§3 and this file's stage diagram/per-stage-detail both updated. Verified end-to-end on the real 65.067s `final.mp4`: output duration 65.067s (0.000% drift), style checked on frames from multiple points in the video, not just the opening shot. | Human request (author): wanted the finished movie in pixel-art style; picked the specific technique after reviewing real rendered samples, not a guess | rpprabha1@gmail.com |
| 2026-07-18 | **Architecture change: 06_fallback_generation reclassified HYBRID → CODE by default (AGENT+diffusion mode kept, opt-in).** After watching the corrected multi-angle render, the author reported the video freezing after ~40s - the real footage timeline (41.6s, only the 3 beats with matched footage) ran ~23.4s shorter than the real audio track (65.1s, narration for all 5 beats), since `ch1_sc1_b003`/`ch1_sc1_b005` never got fallback footage: sd-turbo diffusion loading had failed 6 times this session on this machine's RAM/disk (see the earlier 06_fallback_generation and float16-compute entries above). Author decision: replace sd-turbo with a much lighter fallback rather than keep fighting the memory ceiling. Added `shared/media/ffmpeg_utils.generate_text_card()` - wrapped text over a solid background, rendered directly as a video-length clip via plain ffmpeg (`drawtext` + `color` lavfi source), no ML model, no RAM/disk risk beyond what ffmpeg itself already needs (proven reliable all session). New `config/text_card.yaml` for style tunables (canvas size, colors, font, wrap width). `main()`'s `agent_call` parameter now branches exactly like 07_editorial_direction's 2026-07-18 change: `None` (default) → CODE path, a `_default_text_card_renderer()` call per routed beat using that beat's own `visual_description` directly (no LLM - the diffusion-styled `image_prompt` an agent would write reads badly as on-screen text anyway, so skipping the agent isn't a separate compromise, it's the correct adaptation once the render target changed from "diffusion prompt" to "display text"); an explicit `agent_call` → the original AGENT+diffusion path (prompt-writing agent, `sd-turbo` via `shared/generation/image_backend.py`, Ken Burns zoompan) remains fully implemented and reachable for later revival on a machine with more headroom. `CLAUDE.md`'s classification table and this file's stage diagram both updated. | Human decision (author): actually watching the freeze-frame gap made its real cost obvious in a way the numbers alone hadn't; a lightweight ffmpeg-only fallback closes the gap for real this session instead of continuing to chase sd-turbo's memory requirements on unsuitable hardware | rpprabha1@gmail.com |
| 2026-07-18 | **Fixed `13_pixel_art_conversion`'s downscale + added an outline overlay.** Same-day author feedback on the just-shipped Stage 13: object outlines were unclear/not understandable in places. Root cause, found before reaching for any new tool: `apply_pixel_art_style()`'s downscale used `scale=...:flags=neighbor` (nearest-neighbor point-sampling) for the *shrink* step too, not just the final blocky upscale - point-sampling a downscale picks one arbitrary pixel per block instead of averaging the region, so a block's color could come from a lucky/unlucky single pixel right at a boundary, reading as arbitrary/unclear exactly where the author noticed the problem. Fixed the downscale to `flags=area` (region averaging) and added a dark outline overlay traced by ffmpeg's native `edgedetect` filter computed at the same low grid resolution (so the outline aligns to the pixel blocks), multiplied into the luma channel only via `extractplanes`/`blend`/`mergeplanes` so the underlying color survives (a first attempt using a plain `blend=all_mode=multiply` desaturated the entire frame to grayscale - `edgedetect`'s neutral chroma planes got multiplied straight through - caught by visual inspection before it reached any real output). Evaluated the same "any extra tool worth it?" question the original Stage 13 build asked: still no - ffmpeg's own `edgedetect` already does exactly this, no new install needed. Tried and rejected two more variants against real footage: a softer/higher-threshold outline at reduced opacity (barely distinguishable from the accepted version) and a bolder version (lower threshold + morphological dilation) that over-detected texture as edges and crushed large areas to solid black. `config/pixel_art_spec.yaml` gained `edge_low`/`edge_high`; `shared/media/pixel_art.py`'s `apply_pixel_art_style()` signature and filter chain updated accordingly; `stages/13_pixel_art_conversion/src/run.py` passes the two new params through. All 3 unit tests still pass against the new chain; re-verified end-to-end on the real 65.067s `final.mp4` with zoomed frame comparisons on the specific boundary the author flagged as unclear. | Human feedback (author), same day as the stage shipped: outlines unclear in places; researched whether a dedicated outlining tool was needed first, found the actual bug (point-sampling downscale) and a sufficient ffmpeg-native fix instead | rpprabha1@gmail.com |
| 2026-07-20 | **Raised `13_pixel_art_conversion`'s pixel grid from 160x90 to 320x180.** Author feedback, still on the outline-overlay version: some areas still hard to understand, asked to try a smaller pixel size. Rendered 3 real ~15s samples at finer grids (240x136, 320x180, 480x270), all with the same outline overlay, and reviewed full (not just cropped) frames from two different scenes - a busy textured close-up and a simpler escalator shot - so the choice wasn't based on one favorable crop. 240x136 was a clear improvement over the original 160x90; 320x180 was more legible still while both grids kept the escalator scene equally clean; 480x270 gained little further on the escalator scene and, in the busiest areas of the textured scene, started reading as a photo with an outline filter rather than pixel art. Author picked 320x180. Only `config/pixel_art_spec.yaml`'s `downscale_factor` changed (12 → 6); no code or filter-chain changes were needed since grid size was already a config-driven, resolution-proportional value from the original Stage 13 build. Re-ran the real stage end-to-end on the full 65.067s `final.mp4`. | Human feedback (author): outline overlay alone wasn't enough in busy/textured areas; a finer grid closed the remaining gap without needing a different technique | rpprabha1@gmail.com |
| 2026-07-23 | **Architecture change: new Stage 14, `14_anime_style_conversion` (CODE), added after `13_pixel_art_conversion`.** Author request mid-bulk-run: wanted a real anime-style output using AnimeGANv2 specifically. No record of this existed anywhere in this repo (checked `ARCHITECTURE.md`, `DECISIONS_LOG.md`, `CLAUDE.md`, and prior memory) before this session - the "anime style" 2026-07-18 commit title was confirmed (by re-reading its actual diff) to be a `pixel_art_spec.yaml` grid-size tuning pass only, unrelated to any GAN. Treated as a genuinely new decision starting now, not an assumed prior one. Cloned `bryandlee/animegan2-pytorch` (MIT-licensed; vendored under `shared/models/animegan/` with all 4 of its bundled checkpoints reviewed, only `paprika` shipped). This machine is CPU-only (`torch.cuda.is_available()` false - same constraint already established for sd-turbo); measured real single-frame inference time before committing to anything: 0.38s/frame at 512x288 up to 4.6s/frame at 1920x1080. Rendered 4 real ~18s samples (one per bundled checkpoint) from the real `ch1_sc1` `final.mp4` at 960px width; author picked `paprika`. Separately, testing surfaced that AnimeGANv2 (trained on photographic/portrait content) badly blurs flat text-on-solid-background content toward illegibility - shown to the author with a real before/after frame from an actual text-card beat; author explicitly chose uniform application anyway (not masking fallback-origin clips out) rather than have this stage discover the tradeoff and decide unilaterally. Implemented as a new stage rather than folding into Stage 13 (Rule 1: one stage = one folder - a torch/GAN dependency footprint is a materially different risk profile than Stage 13's ffmpeg-only chain, and keeping them separate means Stage 13 stays untouched/unaffected): `shared/generation/animegan_backend.py` (`stylize_video()` - extract at reduced `stylize_fps`, run each frame through the vendored `Generator`, reassemble at `output_fps` with the source audio), `shared/models/animegan/{model.py, paprika.pt, LICENSE_animegan2-pytorch.txt}`, `config/anime_style_spec.yaml` (checkpoint, device, target width, stylize/output fps - all config-driven per Rule 11), `config/thresholds.yaml`'s new `anime_style.duration_tolerance_pct` (2%, looser than pixel_art's 1% since this stage re-times through a reduced-then-restored frame rate rather than a straight re-filter). `stages/14_anime_style_conversion/src/run.py`'s `main()` accepts an injectable `stylizer` (same DI pattern as Stage 06/07's injectable model calls) so tests never make a live model call. `CLAUDE.md` §2 and this file's stage diagram/per-stage-detail both updated. Real full-chapter timing and duration-drift verification pending at the time of this entry - see the entry directly below (or `DECISIONS_LOG.md`) once available before trusting this for the full 10-chapter run. | Human request (author): wanted a real anime-style output via AnimeGANv2 specifically; picked the checkpoint and the uniform-application tradeoff after reviewing real rendered samples, not a guess | rpprabha1@gmail.com |
| 2026-07-23 | **Quality-improvement batch across 7 author-flagged issues on the real chapter-1 output** (in one change set; per-item human decisions in `DECISIONS_LOG.md`): (1) **Implemented the two approved-but-never-built keyless FootageSources** - `shared/sources/wikimedia.py` (Commons API; per-file license/artist captured from extmetadata, attribution flagged for everything except CC0/PD, >150MB originals skipped) and `shared/sources/archive_org.py` (advancedsearch + per-item metadata API; guardrails learned from real probing: relevance ordering not download-count, `licenseurl:*publicdomain*` restriction, smallest mp4 derivative, >200MB items skipped) - both wired into Stage 03's `_default_sources()` unconditionally (no keys needed). (2) **Beats gained an optional agent-written `search_query`** (`beats.schema.json`, `AGENT_PROMPT.md` sections 4/9) - concrete stock-search phrasing beats mechanical stopword-stripping of `visual_description`; Stage 03 prefers it, falling back to the mechanical extraction. (3) **TTS engine switched Piper -> Kokoro-82M** (`config/tts.yaml` `engine` key, both engines kept; `shared/generation/kokoro_synth.py` runs under Python 3.12 via the `py` launcher in a subprocess since kokoro-onnx needs py>=3.10 and the pipeline runs 3.9) - author found Piper's lessac voice too robotic on the real output; voice picked from 4 real samples. (4) **Stage 08 narration reconciliation rewritten from scale-to-fit to tile-to-fit**: the old proportional stretch demanded 30s+ of continuous footage from 5-30s clips (the dominant real cause of `asset_too_short_for_narration` text-card replacements) and produced one near-static hold per beat; now the narration window is tiled with ~pacing-hold_max segments cycling through the beat's assets with fresh source windows (single long assets yield real visual progression; multiple assets alternate; bounded window reuse before routing to fallback; single generated-card beats keep one unbroken window). (5) **Deterministic edit plan's beat-boundary transition is now config-driven** (`editorial_vocab.yaml` `default_beat_transition: crossfade`); the `repeated_dramatic_transition` HITL check exempts the configured default (it guards an agent's repeated creative choice, which a mechanical default is not), and Stage 08 over-provisions each non-final beat's visual window by its outgoing transition duration so crossfade time-borrowing no longer ends as freeze-frame tail padding. (6) **Text cards upgraded** from flat solid to a slowly-drifting radial gradient (`generate_text_card` `bg_color2`, `text_card.yaml`). (7) **Stage 14 gained an optional Real-ESRGAN upscale step** (`shared/tools/realesrgan/` ncnn-vulkan binary + realesr-animevideov3-x2 model, config-driven in `anime_style_spec.yaml`) - measured for real at ~1.0s/frame batch 960->1920 on the 2GB MX230 GPU (which PyTorch cannot use), ~+75-80 min/chapter at stylize_fps 6. (8) **New approved music source: Jamendo** (`shared/sources/jamendo.py`, LICENSES.md row added same change) - the first music source with a real search API (verified live that Pixabay's API has no music endpoint); all-CC attribution captured per track; orchestrator prefers it when `JAMENDO_CLIENT_ID` exists in config/.env, falling back to the generated sine-bed placeholder otherwise. | Author reviewed the real chapter-1 render end-to-end and flagged 7 concrete quality issues (text-card overuse, robotic TTS, poor narration/visual sync, weak footage search, placeholder music, sub-HD anime output, overall flatness); each remedy chosen against real measurements/samples, not guesses | rpprabha1@gmail.com |
| 2026-07-23 | **Fixed 06_fallback_generation's default visual: on-screen text card retired, replaced with a text-free mood-colored gradient.** Mid-way through the quality-improvement batch above, the author reviewed the actual rendered output and flagged the text cards directly: `09_audio_production`'s Kokoro TTS already speaks each beat's `visual_description` aloud as narration, so the CODE-default fallback visual *also* displaying that exact text on screen was redundant and read as a broken slideshow, not video - not a hypothetical, a defect visible in the real render. Root-caused to the 2026-07-18 text-card design itself, not a new regression. Fix: `shared/media/ffmpeg_utils.py` gained `generate_mood_visual()` (a still radial gradient + the existing `ken_burns_zoompan()` helper already used for the AGENT+diffusion path - a slow zoom over an abstract color field, no text, no new model). `config/text_card.yaml` retired; `config/fallback_visual.yaml` added with a `mood_color_map` (the 18-word mood vocabulary -> gradient color pairs) so different beats/moods read as visually distinct rather than one repeated static color. `stages/06_fallback_generation/src/run.py`'s CODE-default path (`_default_mood_visual_renderer()`), its `main()` signature (`mood_visual_renderer` replacing `text_card_renderer`), and `run_full_novel.py`'s `extend_undersized_assets_for_narration()` (which regenerates an oversized fallback visual when real footage can't cover narration length) all updated to match. `generate_text_card()` deleted outright (no longer called anywhere, including by the AGENT+diffusion path, which never used it). All 10 of Stage 06's unit tests updated (assert on `mood_tags`/colors instead of rendered text) and pass; full 115-test suite green. | Human feedback (author), reviewing a real rendered chapter mid-batch: "I still see images with just narration text in them. This is a bug and needs to be removed" | rpprabha1@gmail.com |
| 2026-07-23 | Fixed `02_beat_extraction`'s `search_query` repetition with strict code, not another prompt tweak. Real run showed the agent assigning the identical `search_query` ("pig speaking barn") to ~18 consecutive beats covering genuinely distinct points of Major's speech - every one of those beats then searched Stage 03's sources for and scored against the exact same handful of stock clips regardless of what the beat was actually about, defeating the point of the per-beat `search_query` field added in the quality-improvement batch above. Per the author's standing direction ("do not fix in prompt.. use the correct best practice for strict rules"), fixed mechanically instead of further prompt hardening: moved `extract_search_terms()` (and its `_STOPWORDS` set) out of `stages/03_candidate_fetch/src/run.py` into a new `shared/text/` module (Golden Rule 2 - Stage 02 now needs the same function, and stages never import each other's `src/`); Stage 03 updated to import from `shared.text` with no behavior change (its own 7/7 tests still pass). Added `_diversify_repeated_search_queries()` to `stages/02_beat_extraction/src/run.py`, run immediately after `_merge_duplicate_visual_beats()`: counts each beat's normalized `search_query`, and for every beat whose query is shared by more than one beat in the scene, mechanically overwrites it with `extract_search_terms(beat["visual_description"])` (the same deterministic fallback Stage 03 already uses when a beat has no `search_query` at all) - so beats sharing a lazy identical phrase get a real per-beat query derived from their own actual content instead. Rewrite count reported in the stage's summary (`"Diversified N repeated search_query beat(s)."`), matching the existing pattern for merged-beat/dropped-tag counts. Added a direct unit test on the helper plus an integration test through `main()`; full 120-test suite green. | Author's standing strict-code-over-prompt-tuning direction, applied here to a previously-diagnosed but unfixed defect | Claude (applying the author's standing policy) |
| 2026-07-23 | Fixed `shared/sources/wikimedia.py`: `WikimediaCommonsSource.name` was `"wikimedia"`, but `candidates.schema.json`'s `source` enum only accepts `"wikimedia_commons"` (matching `LICENSES.md`'s row name). Never caught before now because every prior real run's search queries happened to return zero actual Wikimedia hits, so `_candidate_to_dict()` never emitted the mismatched string into `candidates.json` for schema validation to catch - the freshly-diversified per-beat `search_query` values (see the entry directly above) were specific enough to return a real Wikimedia match for the first time, and Stage 03 crashed the whole scene (`jsonschema.ValidationError`) the instant that happened. Fixed by renaming the class attribute to `"wikimedia_commons"`; added `shared/tests/test_wikimedia_source.py` (new file - this source had zero test coverage previously), which mocks the Commons API response and validates a constructed `candidates.json` payload against the real schema, so this exact mismatch would fail CI rather than only a live run. Full 121-test suite green. | Caught by the ch1 rerun that exercises the search_query diversification fix - a fix surfacing a previously-dormant bug, not a new regression | Claude (root-cause fix) |
| 2026-07-23 | **New downloader-lane scoring stage: `01_2_scene_scoring` (CODE), plus `scene_scores.schema.json` and `config/thresholds.yaml`'s new `scene_scoring.frames_per_clip` (3).** Scores the `01_1_downloader` lane's clips against the scene per beat: embeds each beat's `visual_description`, samples `frames_per_clip` frames from every downloaded clip (via `shared/media.extract_frames`), embeds them through `shared/embeddings/` (same `openai/clip-vit-base-patch32` CLIP as Stage 04), and averages their cosine similarity to the beat text — output is a per-beat best-fit-first ranking ("ranked scores only": every scored clip carries a score+rank, no winner selected, nothing routed). Reads clips via the downloader's `downloader_manifest.json` (produced by `shared/downloader_manifest.py`) and resolves each `file_ref`; a clip that can't be frame-sampled is excluded-and-counted, never fatal (Rule 7). Frames are extracted+embedded once per clip and reused across all beats. **Deliberately source-free, departing from Rule 12 (attribution-as-output):** per the author's explicit "strictly no source" instruction for the downloader lane, `scene_scores.schema.json` carries only a neutral `clip_id`, a `file_ref`, and score/rank — no source/origin/url/license/creator (enforced via `additionalProperties: false`); QA Stage 12's attribution-completeness check would need these clips treated as attribution-exempt (like generated fallbacks) before this lane feeds the main pipeline. Classified CODE per CLAUDE.md §2 (CLIP scoring is math, like Stage 04); `main()` takes an injectable embedder + frame extractor so tests make no model/ffmpeg call. Verified: 7/7 unit tests on synthetic fixtures (deterministic ranking, config-driven frame count, extraction-failure/missing-file exclusion, missing-input FAILED, empty edge cases) + a real end-to-end run of the 3 downloaded "crow throwing stones in bottle" clips against a synthetic 2-beat scene (real ffmpeg sampling + real CLIP; rankings complete/contiguous, top clip sensible for both beats). Consumes only the downloader lane's manifest + output clips, not its script. | Author-requested ("a stage that will score the scene which is best fit"); reference target (beat `visual_description`) and output shape (ranked scores only) confirmed via clarifying questions; source-free design per the author's standing "strictly no source" instruction for this lane | rpprabha1@gmail.com |
| 2026-07-24 | **Fixed `11_assembly_render`'s O(n²) concat: real bug found running the fixed downloader-lane pipeline at its true scale.** `07_2_narration_shot_mapping`'s real output (222 extracted shots for one scene) exposed that the assembly loop built the video via a GROWING chain (`current = transition(current, next_clip)`), re-encoding the ENTIRE ever-longer `current` on every one of 221 clip boundaries - fine at the handful-of-shots scale this was originally tested at, but quadratic in clip count. A real run was still short of finishing after 1.5+ hours with per-step time still climbing (16s → 187s and rising, on track for many more hours) before being caught and stopped. Fixed in `shared/media/assembly.py` (new `concat_stream_copy()`, ffmpeg's concat demuxer with `-c copy` - a lossless O(1)-per-clip stream copy) and `stages/11_assembly_render/src/run.py`: every hard-cut/match-cut-suggestion boundary (the overwhelming majority - all of `07_2`'s intra-beat shot cuts) is now joined for free via stream copy; only real transitions (crossfade/dip-to-black, ~beat boundaries only) still re-encode, and only their own two (small) clips, never a growing chain. `concat_hard_cut()` (filter_complex re-encode) removed, no longer used anywhere. Caught and fixed a second bug while implementing the first: a naive "consume transition pairs, skip by 2" version silently dropped the SECOND clip's own `transition_out` whenever two real transitions occurred back-to-back (a single-shot beat both fading in and immediately fading out) - rewritten to walk every boundary in original order (matching the old code's correctness) while still avoiding the growing-chain cost; bridge cache filenames are content-derived (hash of the two input filenames, which are themselves content-derived) rather than position-based, mirroring the exact fix already applied to `normalize_clip`'s cache on 2026-07-18 for the identical staleness failure mode. Verified at real scale (the actual 222-clip/39-crossfade `ch1_sc1` timeline): **8.3 minutes**, `final.mp4` duration 837.033s vs audio target 837.014s (0.002% drift) - versus the old code's multi-hour, incomplete trajectory. Also fixed in the same investigation: `07_2_narration_shot_mapping` was hardcoding every beat's `transition_out` to `hard-cut`, silently dropping `config/editorial_vocab.yaml`'s `default_beat_transition: crossfade` (a real 2026-07-23 author-requested fix - "uniform hard-cuts read visually flat") - now reads it, matching `07_editorial_direction`'s deterministic path. 10 new/updated tests (2 assembly regression tests pinning both the O(n²) fix and the double-transition bug; 1 new `07_2` test for the transition-default fix); full 147-test suite green. | Caught running the real downloader-lane pipeline at true scale (222 shots) - the bug was invisible at every smaller scale this stage had previously been tested/run against | Claude (root-cause fix, verified at real scale before resuming the run) |
| 2026-07-24 | **Architecture change: explicit shot extraction — new stage `07_2_narration_shot_mapping` (CODE), plus clip-collection fix and two downloader-invoke fixes.** Follow-up to the same-day cutover below, after a real run exposed three problems. (1) **Clip collection was collapsing a whole scene onto one clip.** `run_full_novel.download_scene_clips` copied only clips a before/after snapshot of the downloader `outputs/` folder showed as *newly* downloaded into a per-scene dir - but the one-shot downloader skips re-downloading a title already present, so on any run with clips already in `outputs/` (e.g. after a prior stopped run) the "new" set was nearly empty and the scene ran on a single clip. Replaced with `download_scene_queries` (runs each unique beat query to populate the shared pool) + building `downloader_manifest.json` from the **whole** `outputs/` pool; `01_2_scene_scoring` ranks per beat, so off-topic clips simply lose ("strictly stick to the downloader's output"). (2) **Two downloader-invoke fixes** in `_default_downloader_invoke`: feed `"3"` (480p) not the Best-quality default on stdin - a real run pulled multi-hundred-MB/multi-GB full videos for a few seconds of beat coverage; and set `encoding="utf-8"/errors="replace"` on the subprocess - `subprocess`'s own reader threads decoded the downloader's non-ASCII stdout with the parent's cp1252 default and raised `UnicodeDecodeError` (downloads themselves were fine). (3) **New Stage `07_2_narration_shot_mapping`** (author request: the downloader clips are long, "we might only need 4-5 seconds of shot in them ... add another stage that extracts shots from videos ... narration to shot mapping"). It runs **after `09_audio_production`** (narration length is authoritative): for each beat it takes that beat's top-`downloader_selection.assets_per_beat` ranked clips, tiles the beat's narration duration into ~shot-length windows (shot length = the active pacing preset's `hold_duration_s.max`, override via new `config/thresholds.yaml` `shot_extraction`), and **physically extracts** each window as its own short `.mp4` via new `shared/media.trim_clip`, greedily dropping any clip whose extraction fails (undecodable input) and continuing coverage from the rest so a bad clip never under-covers a beat while decodable footage remains. Outputs `shot_map.json` (new `shot_map.schema.json` - the explicit narration→shot mapping), a source-free `assets_manifest.json` (each extracted shot an asset), and `edit_plan.json` (hard-cut transitions). **Orchestrator reordered** to `02 → [download → downloader_manifest → 01_2_scene_scoring] → 09 → 07_2 → 08`; `08_timeline_builder` is now called **without** `audio_mix.json` (07_2 already mapped shots onto narration, so 08 lays them out as-is rather than re-tiling). `07_editorial_direction` and the `shared/downloader_assets.py` bridge are **retired from the flow** (kept in-tree, no longer invoked); `run_full_novel`'s `extend_undersized_assets_for_narration` mood-visual path (the last synthetic-visual generator) removed with them - the pipeline is now zero-synthetic. `12_qa_attribution` treats `shot_map.json` as an optional validated artifact. Verified: 144-test suite green + a real-ffmpeg smoke test of the new stage on 4 downloaded clips (incl. one undecodable clip ranked first) fully covered an 11s narration from the remaining clips. | Author request ("strictly stick to the downloader's output"; "add another stage that extracts shots ... narration to shot mapping"); the three fixes each address a defect observed on the real ch1_sc1 run | rpprabha1@gmail.com (requests); Claude (diagnosis + implementation) |
| 2026-07-24 | **Architecture change: downloader-lane cutover — YouTube forbidden-source override, source-free lane becomes the sole footage source, stock lane (03/04/05) and synthetic fallback (06) retired.** Author explicitly overrode `LICENSES.md`'s forbidden-source rule to use the `01_1_downloader` (YouTube) lane as the pipeline's only footage source (the exact request declined twice on 2026-07-21; see `DECISIONS_LOG.md`'s 2026-07-24 row for the override and the auto-invoke / retire-keep-folders sub-decisions). Claude flagged the conflict and refused to wire it in silently before the override was on record. Change set: (1) **New bridge `shared/downloader_assets.py`** — `build_assets_manifest()` turns `01_2_scene_scoring`'s per-beat `scene_scores.json` ranking + the source-free `downloader_manifest.json` (for clip durations, which `scene_scores` doesn't carry) into a schema-valid `assets_manifest.json`, selecting the top-N ranked clips per beat. Stays source-free end to end: `origin: "downloader"`, a neutral placeholder `license`, `attribution: {source: "downloader", creator_required: false}` — no url/channel/creator/real-license ever attached (passes QA's attribution check precisely because `creator_required` is false and a license string is present). (2) **`assets_manifest.schema.json`** `origin` enum gained `"downloader"` (+ `SCHEMAS.md` updated). (3) **`config/thresholds.yaml`** gained `downloader_selection.assets_per_beat` (3) — the downloader-lane analogue of `retrieval_verification.assets_per_beat`. (4) **`run_full_novel.py` rewired**: per scene it now auto-invokes `01_1_downloader` once per beat (one-shot CLI form per `downloader_usage.md`, quality-default fed on stdin — the downloader is treated as an opaque black box, never read/imported), catalogs the new clips via `shared/downloader_manifest.py`, runs `01_2_scene_scoring`, and bridges to `assets_manifest.json` — replacing the entire `03→04→05→06` block. (5) **Stage 06 retired to a disabled no-op** (`main()` always COMPLETE, generates nothing; README banner + docstring updated) — the author's "strictly remove the backup option … not needed at all". (6) **Stage 12 QA** now treats `candidates.json` and `scene_scores.json` as optional (validate-if-present) since a run uses one footage lane or the other. Stages 03/04/05 and their Pexels/Pixabay/Wikimedia/Archive sources are left in place (retire-keep-folders) but no longer invoked by the orchestrator. `run_full_novel.py`'s narration-length reconciliation (`extend_undersized_assets_for_narration`, a distinct Stage-08 safety net that regenerates a mood visual when a real clip can't cover its narration) is intentionally left in place and flagged as the one remaining synthetic-visual path. | Author explicitly authorized the forbidden-source override for this project (accepting YouTube's redistribution/third-party-copyright implications for published output — a call only the author can make); Claude flagged the conflict, refused silent integration, and implemented the cutover once authorized | rpprabha1@gmail.com (override); Claude (flag + implementation) |
| 2026-07-23 | Fixed `run_full_novel.py`'s `resolve_music_stage()`: real run crashed with `StageResponse(FAILED, "hitl_decisions references track_ref 'jamendo_682351' not among cue 'cue001''s candidates")` - the retry loop records a cue_id's chosen track_ref from one attempt's options, then resubmits it on the next `main09(...)` call, but Stage 09 regenerates its ENTIRE cue-sheet (a fresh, non-deterministic LLM call) AND re-runs the live Jamendo search unconditionally on every single invocation, with no caching anywhere - so a track_ref that was a real option on attempt 1 can legitimately not exist among attempt 2's freshly re-searched candidates for the same cue_id, once real per-beat mood_tags (from the search_query diversification fix above) started varying enough for this race to actually bite. Root-cause fix, not a symptom patch: added `_CachingMusicSource` (memoizes `.search(mood_tags, max_results)` by exact query for the life of one scene's resolution, so a live source can't return different results for an identical query within one sequence) and `_frozen_cue_sheet_agent_call()` (once an attempt's cue-sheet first clears Stage 09's own coverage/schema validation, `music_cue_intent.json` is read back and replayed verbatim via a custom `agent_call` for every later call in the same sequence, so cue_ids/mood_tags can no longer drift mid-resolution either). Kept the previous bounded drop-stale-and-retry logic as a belt-and-suspenders safety net (now expected to rarely trigger) rather than removing it. Added `tests/test_run_full_novel.py` (new file - this repo-root orchestrator had zero test coverage previously): direct unit test of `_CachingMusicSource`, a test proving the cue-sheet freeze actually engages and is replayed, and a test of the safety-net retry path with a fake that simulates the live-search-drift race directly. Full 125-test suite green. | Same real ch1 rerun, next stage after the wikimedia fix above - author's standing strict-code-over-guessing policy applied to a genuinely new failure mode, not previously seen with the synthetic GeneratedMusicSource placeholder | Claude (root-cause fix) |
